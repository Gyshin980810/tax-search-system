# 환경 변수 (API 키, 시크릿 포함 — 절대 커밋 금지)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일 (개인정보 포함 가능)
logs/*.json
logs/*.log

# 빌드 결과물
.next/
out/
dist/
build/

# 의존성
node_modules/

# OS 및 에디터
.DS_Store
Thumbs.db
.idea/
.vscode/

# 로컬 Claude Code 설정 (개인 예외 규칙)
.claude/settings.local.json

# Next.js
next-env.d.ts
*.tsbuildinfo

# 테스트 커버리지
/coverage

# Vercel
.vercel

# 인증서
*.pem

# 골든셋 작성 보조 — 자동 생성 초안(검수 전, 회귀 게이트 비대상) (TAX-028)
eval/golden_direct.draft.json

# 판례 변환 결과물 (대용량, 적재용 중간 산출물 — git 미커밋) (TAX-6B-13/16)
scripts/precedent_poc.json
scripts/precedent_full.json

# 적재·진단 임시 파일 (TAX-6B-16)
scripts/_*.mjs
scripts/_*.log

# 심판례 전량 수집 산출물 (대용량, 적재용 중간 산출물 — git 미커밋) (TAX-6B-18)
scripts/tribunal_full.json
scripts/tribunal/
scripts/embed_case_number_issues.json

# 로컬 논의용 메모 (git 미추적 — 로컬에서만 사용)
세법 검색 및 RAG 시스템 아키텍처 가이드.md

# 서드파티 MCP 도구 (로컬 복사본, 외부 의존성)
mcp-shrimp-task-manager/
claude-agents-main/

# 테스트 결과물
test-results/

# 로컬 MCP 설정 (기기 종속 절대경로 포함)
.mcp.json

# Playwright 인증 상태 (세션 쿠키 포함 — git 제외)
.auth/

.vercel

# Codex 툴링 (로컬 전용 — AI 에이전트 설정, git 미추적)
AGENTS.md
.codex/
.agents/
