# [TAX-6B-20-D] 코드 수정 지시서 — R1

> ✅ **결함 없음 — 수정 불필요.** 🔴(치명) 0건 · 🟡(경미) 0건 · 🟢(참고 관찰) 3건.
> 이 티켓은 **순수 문서 정합 티켓**이며(코드 로직 변경 명시 금지 — 티켓 §3.2), 유일한 코드 파일
> `src/adapters/nationalTaxLaw.ts`는 **JSDoc 주석 블록만** 변경됐고 실행 로직·타입·시그니처는
> 전혀 바뀌지 않았음을 working tree diff로 직접 확인했다. 문서(SSOT/PRD/CLAUDE.md/ROADMAP) 정합·
> 과거 오기록 정정(원문 보존 + 날짜 있는 정정 주석)·범위 엄수·시크릿 비노출 모두 준수.
> 아래는 **Codex가 고칠 것이 아니라** quality-gate·회계사 참고용 관찰(🟢) 3건뿐이다.

## 이 문서의 사용법 (Codex에게)
**이 라운드에 수정할 항목은 없다.** 아래 "절대 규칙"과 "검증된 사항"을 확인만 하고,
"참고 관찰(🟢)"은 정보 제공용이므로 별도 지시가 없는 한 손대지 말 것.
특히 이 티켓은 **문서 전용**이므로 어떤 코드 로직·타입도 건드리지 말 것(섣부른 수정이 오히려 규칙 위반).

## 절대 규칙 (이 프로젝트 불변 제약)
- 법령/판례/예규/해석례 원문 텍스트를 요약·의역·가공하지 말 것. 발췌는 원문과 문자 단위로 일치해야 함.
- 과거 티켓·리포트의 **원문(역사 기록)을 고쳐 쓰지 말 것.** 정정은 "원문 보존 + 상단/직후 날짜 있는
  정정 주석 추가" 방식만 허용(티켓 §0 "역사 다시 쓰기 금지").
- 계층 구조 준수: UI → API Route → Usecase → Adapter/Port. Usecase에서 fetch·DB 직접 호출 금지.
- 이 티켓(TAX-6B-20-D)은 **문서·주석 전용**이다. 어댑터/유스케이스 실행 로직·타입·시그니처를 손대지 말 것
  (티켓 §3.2). 실시간 어댑터 `content=''` 정책·V1~V6·라벨 정책도 변경 금지.
- 티켓 범위 밖 파일 수정 금지. API 키·`DATABASE_URL`·`VOYAGE_API_KEY` 값·`OC` 운영키·주민/사업자번호·
  회계사 식별자를 코드·로그·에러·URL·문서에 넣지 말 것(환경변수 "이름" 표기는 허용).

## 티켓 요약
- 목표: 국세청 세법해석례 본문 코퍼스(TAX-6B-20-A~C로 구축)를 SSOT·PRD·CLAUDE.md·ROADMAP에 명문화하고,
  과거 "본문 API 없음/목록만/수십~수백자" 오기록(TAX-6B-19 티켓·리포트, 부모 TAX-6B-20 §2.1)을
  **원문 보존 + 정정 주석** 방식으로 정정. 코드 로직 변경은 명시적으로 금지(주석 정정만 허용).
- 이번 라운드 최우선(R1): 전체 결함 해소. → **결함 없음이 확인되어 수정 대상 없음.**

## 수정 항목
**(없음 — 결함이 발견되지 않았다.)**

---

## 검증된 사항 (quality-gate가 PASS 판단 시 근거로 사용)

### [핵심] AC6 — 코드 파일은 `nationalTaxLaw.ts` 주석만 변경, 로직·타입·시그니처 무변경 ✅
- `git diff src/adapters/nationalTaxLaw.ts` 결과: 변경 라인이 **전부 `/** ... */` JSDoc 블록 내부**
  (`searchNtsInterpretations` 함수 위 문서 주석, 925~933행 부근). 실행문·타입·시그니처·import 토큰
  변경 0건. `export class`·함수 본문·반환 정규화 로직 모두 그대로.
- 주석 내용도 규칙 정합: "국세청 해석은 본문이 없다"는 **오해**만 "실시간 경로는 P95 보호를 위해
  의도적으로 목록만 유지, 본문은 오프라인 코퍼스(TAX-6B-20-A~C)로 확보"로 정정. 실시간 어댑터를
  바꾸라는 지시가 아님을 명시(SSOT §7.2-a 상호 참조). → 티켓 §3.2 "코드 로직 무변경" 준수.
- 리포트가 주장한 `typecheck 0에러 / vitest 829/829`는 **주석-only 변경이므로 컴파일·테스트에 영향
  불가**하다는 구조적 사실과 일치(별도 실행 없이도 논리적으로 무회귀 확정).

### [핵심] AC6/범위 — 건드린 파일이 전부 문서 + 그 1개 주석, 의도치 않은 파일 없음 ✅
- `git status` working tree 변경: `CLAUDE.md`·`ROADMAP.md`·`docs/PRD.md`·`docs/SSOT.md`·
  `docs/reports/TAX-6B-19_report.md`·`docs/tickets/TAX-6B-19_interpretation_list_only.md`·
  `docs/tickets/TAX-6B-20-D_...md`·`docs/tickets/TAX-6B-20_...corpus_load.md`·
  `src/adapters/nationalTaxLaw.ts`(주석만) + 신규 `docs/reports/TAX-6B-20-D_report.md`.
- **테스트 스냅샷·다른 소스 파일·설정 파일 혼입 없음.** 코드 파일은 정확히 1개(주석만). 모두 티켓
  §2.1 갱신 대상 표·§3.1 허용 범위 내. → CLAUDE.md §9 6번(범위 엄수)·7번(최소 변경) 준수.
- 메모리(`project_nonlaw_interp_tracks.md`·`MEMORY.md`)는 git 저장소 밖(`~/.claude/...`)이라 diff에
  안 잡히는 게 정상. 리포트가 갱신을 명시했고 티켓 §3.1이 "접근 불가 시 리포트 명시로 충분"을 허용.

### AC3 — 과거 오기록 정정: 원문 보존 + 날짜 있는 정정 주석 방식 ✅
- **TAX-6B-19 티켓**: 상단에 `> ⚠️ 정정 주석 (2026-07-13, TAX-6B-20-D)` 추가. 원문 §1.1 "본문 조회
  API가 존재하지 않아"(30행)는 **그대로 보존**. 정정 주석이 "공식 API는 존재·작동하나 운영키 본문
  권한 미신청·신청 봉쇄 → 실시간 경로에서만 못 씀 + TAX-6B-20-A~C가 오프라인 코퍼스로 본문 확보"로 교정.
- **TAX-6B-19 리포트**: 상단에 동일 형식 정정 주석 추가, 원문 "본문 조회 API 자체가 없어"(22행) 보존.
- **부모 TAX-6B-20 §2.1**: "해석례 본문은 수십~수백자 수준"(86행) 원문 보존 + 직후 정정 주석("실측
  평균 1,869자·최장 32,991자, MAX_CONTENT_CHARS 6,000→30,000 상향 경위").
- `nationalTaxLaw.ts` 주석: 위 [핵심] 항목대로 오해만 교정.
- → 티켓 §0 "역사 다시 쓰기 금지" + CLAUDE.md §6.1 원문 보존 철학과 동일 원리 준수. AC3의 완료 기준
  (2026-07-11 정정: "정정 주석 없이 현재 사실처럼 단정하는 오기록 0건")을 4곳 모두 충족(원문은
  주석으로 무효화됨).

### AC3 — "운영키 권한 봉쇄" 서술이 기존 검증된 역사 기록에 근거(새로 지어낸 것 아님) ✅
- 정정 주석·SSOT §7.2-a·PRD "FR-10 상태 보충 2"의 핵심 주장("공식 API는 존재·작동하나 운영키 본문
  권한 미신청·신청 봉쇄, 테스트키 OC=data로 12/12 성공")은 **부모 티켓 TAX-6B-20 §0 전제 변천사 표
  (27~31행)·§8(공식 API 경로 봉쇄)와 문자적으로 일치**한다:
  - §0 표 2026-07-08 오전: "공개 테스트키(OC=data)로 lawService.do?target=ntsCgmExpc&type=XML 12/12 성공"
  - §0 표 2026-07-08 오후(최종): "운영키 본문 권한 🔒 미신청 + 신청 자체가 막힘(회계사 확인)"
  - §8: "공식 본문 API는 존재하고 작동한다(테스트키 12/12) ... 운영키 본문 권한이 미신청이고 신청이 봉쇄"
- → **새로 지어낸 서술이 아니라 이미 검증된 역사 사실의 재기술.** CLAUDE.md §6.1(무근거 서술 금지) 준수.

### AC1/AC2 — 문서 위계 순서(SSOT→PRD→CLAUDE.md) 정합, 상호 모순 없음 ✅
- **SSOT** §7.2-a 신설(국세청 해석례 본문 코퍼스: 크롤링 확보·135,907건 voyage-4 적재·`VECTOR_REFERENCE_GATES`
  병렬 편입·references 전용·V1~V6 비대상·citation 승격 금지·실시간 어댑터 무변경). §7.2 매핑 표 content
  각주도 "실시간은 의도적 목록 유지, 오프라인 코퍼스 별도 존재"로 보강.
- **PRD** FR-10 티켓 목록에 `20-A~C` 추가 + "해석례 처리 정합(TAX-6B-19)" 블록쿼트에 v2.9 보충 +
  "FR-10 상태 보충 2" 블록쿼트 신설(SSOT와 동일 결론).
- **CLAUDE.md** §7.1 `VOYAGE_API_KEY` 용도에 "국세청 해석례 벡터 적재(TAX-6B-20-B)" 추가.
- 세 문서 모두 "국세청 해석례 = 참고 목록 전용 · V1~V6 비대상 · citation 승격 금지 · 본문은 오프라인
  코퍼스"로 **결론 일치**. 상위→하위 순서로 반영돼 위계 위반 없음.

### AC5 — changelog 행 + 헤더 버전·갱신 이력 라인 함께 갱신(문서 내부 일관성) ✅
- **SSOT**: 헤더 `버전: 2.7 → 2.9`, 갱신 이력 라인에 `2026-06-22 (v2.8)`·`2026-07-13 (v2.9)` 추가
  (기존에 헤더는 v2.7까지인데 changelog엔 v2.8 행이 이미 있던 **선존 불일치를 소급 보정**),
  changelog에 v2.9 행 신설. → 헤더·changelog 상호 일치 회복.
- **PRD**: 헤더 `버전: 2.8 → 2.9`, 갱신 이력 라인 + changelog v2.9 행 추가. 일관.
- 두 문서의 신규 changelog 행이 "코드 변경 없음(주석 정정만)"·"실시간 어댑터·V1~V6·라벨 무변경"을 명시.

### AC7 — SSOT 비법령 식별자 서술에 `externalId` 반영, "caseNumber 단독 식별" 잔재 없음 ✅
- **TaxLaw 필드 표(현 244행)**: "비법령 식별자(caseNumber)…" 비고에 `TaxLaw.externalId`(2026-07-10 신설)
  반영 — 2004년 이전 국세청 해석례 caseNumber 세목명 공유("재산" 82건, 20-B 실증) 근거 포함,
  **dedup·동일성=externalId 우선·caseNumber 폴백**, V1 대조는 caseNumber 유지(§7.4 리스크 참조).
- **V1 규칙(현 306행)**: 직후에 V1 잔여 리스크 각주 추가(2004년 이전 caseNumber 충돌 시 오매칭 가능성,
  낮은 확률·문서화만·유의미해지면 별도 티켓 승격).
- 정규화 매핑 표(현 268행)의 caseNumber 행은 순수 **필드 매핑 규칙**("원문 보존")이라 식별자·dedup을
  단정하지 않으므로 externalId 각주 대상이 아니다(관찰 3 참고). → "caseNumber를 유일 식별자로 단정"하는
  잔재는 SSOT에 없음. AC7 실질 목표(단정 잔재 0건) 충족.

### AC4/ROADMAP + 시크릿 ✅
- ROADMAP §3 해석례 트랙 행을 `🟡 20-C 라이브 검증 완료` → `🟢 20-A~D 완료`로 갱신, 잔여 목록에서
  20-D 제거하고 20-E(조건부)만 남김. 이중 반영 없음(§692/§697 상호 참조 갱신).
- 모든 diff에 API 키·토큰 **값**·회계사 식별자·주민/사업자번호 노출 없음. `NATIONAL_TAX_API_KEY`·
  `VOYAGE_API_KEY`는 환경변수 "이름"으로만, `OC=data`는 공개 데모키(부모 §0 기록된 공개 사실)로만 언급.

---

## 참고 관찰 (🟢 정보 제공 — Codex 수정 대상 아님, quality-gate·회계사 판단용)

### (관찰 1) 20-D 티켓 자체 §0/§2.1 계획 표는 "최대 26,886자"(옛 값), 실제 정정 주석은 "최장 32,991자"(최신)
- 20-D 티켓 §0 정정 대상 표(34행)·§2.1 표는 계획 작성 시점(2026-07-09) 수치인 "최대 26,886자"를 인용한다.
  반면 **부모 20 §2.1에 실제로 붙인 정정 주석은 20-B 리포트 정정 후 값인 "최장 32,991자"**를 쓴다
  (SSOT/ROADMAP의 "30,000자 초과 2건·최대 32,991자"와 일치). 즉 **납품된 정정 문구는 최신·정확**하고,
  옛 값은 이 티켓 자체의 계획 서술에만 남아 있다. 실질 결함 아님 — 티켓 계획 문서의 사소한 잔존값일 뿐.
  건드릴 필요 없음(계획 문서는 역사 기록 성격).

### (관찰 2) 범위 밖 과거 티켓들(TAX-016B/C·TAX-018·TAX-6B-21)에 여전히 "본문 없음/목록만" 프레이밍
- 이들은 **이 티켓의 정정 대상이 아니다**(티켓 §2.1은 TAX-6B-19 티켓·리포트 + 부모 20 §2.1로 한정).
  범위 밖 파일을 손대면 오히려 CLAUDE.md §9 6번(범위 엄수) 위반이고, "역사 보존" 철학상 옛 완료 티켓을
  다시 쓸 이유도 없다. → **손대지 않은 것이 올바른 선택.** 향후 대규모 문서 일괄 정합(`/sync-docs`) 시
  참고용으로만 기록. 이번 티켓의 결함 아님.

### (관찰 3) 정규화 매핑 표(SSOT 268행) caseNumber 행에는 externalId 각주가 직접 붙지 않음
- 리포트/티켓 AC7이 "244·268행"을 언급하나, 실제 externalId 우선·폴백 정책 각주는 **식별자 성격의 표
  (244행 TaxLaw 필드 표) + V1(306행)**에 정확히 들어갔다. 268행은 "외부 필드→도메인 필드" 순수 매핑
  규칙이라 식별자/ dedup을 단정하지 않으므로 각주가 불필요하다(오히려 넣으면 매핑 표 성격과 안 맞음).
  → AC7의 실질 목표("caseNumber 단독 식별 단정 잔재 0건")는 충족. 리포트의 "268행" 표기는 위치를
  가리키는 서술상 표현일 뿐 누락이 아니다. 코드/문서 수정 불필요.

## 완료 확인 체크리스트 (수정 항목이 없으므로 확인만)
- [x] 🔴·🟡 결함 0건 — 수정할 항목 없음
- [x] 코드 파일은 `nationalTaxLaw.ts` 1개, **주석 블록만** 변경(실행 로직·타입·시그니처 무변경) — diff 직접 확인
- [x] 과거 오기록 정정이 **원문 보존 + 날짜 있는 정정 주석** 방식(역사 다시 쓰기 없음)
- [x] "운영키 권한 봉쇄" 서술이 부모 §0/§8 기존 검증 기록에 근거(무근거 서술 아님)
- [x] SSOT↔PRD↔CLAUDE.md 결론 상호 정합 + 헤더 버전·changelog 함께 갱신
- [x] 티켓 범위 밖 파일·의도치 않은 파일(스냅샷·소스) 혼입 없음, 시크릿 값 미노출
