# TAX-6B-35 구현 리포트 — 세법 판례 주간 증분 수집 (참조법령 JO 필터)

**작성일**: 2026-07-04 | **상태**: 구현 완료 | **티켓**: docs/tickets/TAX-6B-35_precedent_incremental_sync.md

---

### 변경 사항 요약

**파일 변경 목록:**
- `scripts/collectPrecedent.ts` (신규) — 판례 증분 수집기
- `tests/unit/collectPrecedent.test.ts` (신규) — 순수 함수 테스트 19건
- `package.json` (수정) — `collect:precedent` 스크립트 추가
- `.gitignore` (수정) — 증분 산출물·판례 원장 미커밋 패턴 추가
- `docs/tickets/TAX-6B-35_precedent_incremental_sync.md` (수정) — 결정·상태 반영

**주요 변경:**
- **참조법령(JO) 필터 방식** (회계사 제안·실측 검증): `lawSearch.do?target=prec&JO=<세법명>`이
  참조조문 기준으로 판례를 정밀 필터링함을 확인(법인세법 1,521건, 엉터리 법령명 0건).
  사건종류 분류가 민사·일반행정이어도 세법을 참조하면 포착된다.
- JO 목록: **국세 8법 + 지방세 3법 × (법률+시행령) = 22개 질의** (회계사 확정,
  `TAX_LAW_JO_BASE` 상수 — 한 줄 추가로 확장 가능).
- **사건번호 정규화 토큰 dedup**: 기존 적재분(precedent-kr)과 API의 본문이 달라
  content_hash가 무력하고 표기도 이원적("2025두36013" vs "대법원-2025-두-34754")이라,
  `normalizeCaseTokens`(하이픈·공백 제거 후 "연도+부호+번호" 추출)로 DB·원장과 대조.
  병합 사건("2013구합59576, 2014구합67529(병합)")은 토큰 하나만 일치해도 기지 판정
  (중복 적재 방지 우선).
- 본문은 법원 출처만 수집(국세법령정보시스템 출처는 본문 미제공 — 어댑터와 동일 판정),
  판시사항+판결요지 원문 그대로(§6.1). 첫 실행은 자동 백필 겸용.

**검증 결과:**
1. `npm run typecheck` — 0 오류
2. `npm run test` — vitest **718/718 통과**
3. **실 API·DB 스모크**(`--max 5`, 2026-07-04): DB 기지 9,962건 로드 → 첫 JO(국세기본법)
   1페이지에서 신규 32건 감지, **사건종류 분포 민사 16 · 일반행정 13 · 형사 2 · 세무 1**
   — '세무' 분류 필터였다면 32건 중 1건만 잡혔을 것(JO 방식의 효과 실증) →
   5건 본문 수집 성공(2025다210837 부당이득금 등, 대법원 2026년 선고 포함)
4. §7 키 보호 — 산출물 전체에 `OC=` 미노출, sourceUrl은 `precInfoP.do` 키 없는 링크
5. §6.1 — content 원문 그대로 결합(테스트로 문자 단위 단언)

**잠재 위험:**
- 토큰 dedup은 보수적(하나만 일치해도 기지) — 병합 신건이 구건과 번호 일부를 공유하는
  극단 사례에서 놓칠 가능성(드묾). 반대급부로 중복 적재는 차단.
- JO 목록에 없는 세법(관세법 등)만 참조하는 판례는 미수집 — 회계사 결정으로 제외,
  필요 시 배열 확장.
- 첫 실행(백필)은 신규 수천 건 예상 — 임베딩 전 건수·비용 확인 후 진행 권장.

**주간 실행 루틴(수동, 매주 1회):**
```
npm run collect:precedent
npm run embed -- --input scripts/precedent_incremental_<YYYYMMDD>.json
```

**리포트:** docs/reports/TAX-6B-35_report.md (본 문서) · 짝 티켓: TAX-6B-34(심판례)
