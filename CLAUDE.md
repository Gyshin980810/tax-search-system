# CLAUDE.md — 세법 API 검색 시스템 (v2.0)

> 이 파일은 AI(Claude Code, Cursor, ChatGPT 등)가 이 프로젝트에서 작업할 때
> **가장 먼저 읽어야 하는 1페이지 행동 지침**입니다.
>
> 문서 위계: `docs/SSOT.md`(헌법) > `docs/PRD.md`(제품 사양) > **`CLAUDE.md`**(행동 요약) > 티켓
> 충돌 시 상위 문서가 우선합니다.
>
> 갱신: 2026-06-08 — §9 8번 보강(승인 요청 시 선택지 장단점 한 줄 설명 의무화, 메모리 feedback_explain_before_decision.md와 정합)

---

## 0. 작업 시작 시 30초 체크 (모든 작업 전 필수)

| 질문 | 답이 "아니오"라면 |
|---|---|
| 티켓이 있는가? | 인간에게 티켓 작성 요청 |
| SSOT·PRD를 읽었는가? | 먼저 읽기 (해당 섹션만이라도) |
| 변경 범위가 티켓에 명시되었는가? | 인간에게 범위 확인 |
| 답변 생성 코드를 만지는가? | §6.4 검증 V1~V6 통과 경로 확인 |
| 법령 원문을 변형하는가? | **즉시 중단** (§6.1) |

---

## 1. 프로젝트 한 줄 정의

**국세법령·지방세법령·판례·예규를 자연어로 질문하면 정확성을 보장한 답변을 회계사에게 전달하는 RAG 기반 세법 검색 어시스턴트.**

- 1차 사용자: 회계사·세무사 (50명 이내, 의뢰인 의사결정 보조 용도)
- 개발: 회계사 1인 + AI 협업
- 기술: Next.js (App Router) + TypeScript + Tailwind CSS + Vercel AI SDK
- LLM: GPT-4o-mini (실증·운영)

---

## 2. 핵심 원칙 (흔들리지 말 것)

> **정확성 > 완전성 > 속도 > 편의성**
>
> **"틀린 답은 없는 답보다 나쁩니다."**

회계사가 본 시스템의 답변을 의뢰인 보고서·세무 신고에 인용합니다. 환각·의역·시점 오류 1건은 가산세·법적 분쟁으로 직결됩니다. 따라서 모든 결정은 정확성을 보장하는 쪽을 따릅니다.

**책임 분리:**
| 주체 | 책임 |
|---|---|
| 본 시스템 | 공식 API 검색 결과의 정확한 인용·라벨링·시점 표기 |
| 회계사 | 사실관계 검토, 법령 적용 판단, 의뢰인 응대 |
| 외부 API 제공기관 | 법령 원문의 정확성 |

---

## 3. 언어 및 커뮤니케이션

- 응답·주석·커밋 메시지·문서: **한국어**
- 변수명·함수명: **영어 (camelCase)**

---

## 4. 계층 아키텍처 (절대 어기지 말 것)

```
UI (app/)  →  API Route (app/api/)  →  Usecase (src/usecases/)  →  Adapter (src/adapters/)
                                              ↑
                                      Port (src/ports/) — 인터페이스 분리
```

| 계층 | 책임 | 금지 |
|---|---|---|
| UI | 표현·입력 | 비즈니스 로직 |
| API Route | 검증·인증·매핑 | 외부 `fetch`·DB 호출 |
| Usecase | 워크플로우·오케스트레이션 | `fetch`·DB 직접 호출 (Port만 사용) |
| Adapter | 외부 I/O 정규화 | 비즈니스 판단 |

---

## 5. RAG 5단계 파이프라인 (단계 압축·생략 금지)

```
[1] 자연어 쿼리 변환 (LLM 1차)
       ↓
[2] 외부 API 검색 (국세·지방세 + 향후 벡터 DB)
       ↓
[3] 답변 생성 + 라벨링 + Trust Tier (LLM 2차)
       ↓
[4] law-verifier 검증 (V1~V6, 필수 — §6.4)
       ↓
[5] 회계사 화면 출력
```

- ❌ **단계 임의 압축·생략 금지** (메모리 `feedback_pipeline_steps.md`)
- ❌ **검증[4] 우회 금지** — 검증 미통과 답변은 회계사에 노출 불가
- ✅ 통합·생략 제안은 별도 티켓으로 분리해 인간 승인 받기

---

## 6. 세법 도메인 정확성 4대 규칙 (v2.0 핵심)

### 6.1 인용 무결성 (Citation Integrity)

- ❌ 법령 본문을 임의로 요약·가공·**의역하지 말 것**
- ✅ 발췌 인용은 외부 API 원문과 **문자 단위로 일치** (퍼지 매칭 금지)
- ✅ 부분 인용 시 생략 표시는 `(…)`로 통일
- ✅ 조문 번호·항·호 표기는 원문 형식 유지 (`제26조 제1항 제12호`)
- ✅ 검색 결과에는 항상 **원문 링크** 포함

### 6.2 출처·시점·Trust Tier

**Trust Tier (높을수록 우선):**

| Tier | 출처 | 라벨 활용 |
|---|---|---|
| **T1** | 법률·시행령·시행규칙 본문 | 🟢 직접 근거 1순위 |
| **T2** | 법령 부칙·경과조치 | 🟢 직접 근거 (시점 분기 시 필수) |
| **T3** | 국세청 예규·해석례·법령해석례·기재부 회신·**조세심판원 결정례(심판례)** | 🟡 유사 사례 (사안 일치 시 🟢 가능) |
| **T4** | 대법원·헌법재판소 판례 | 🟡 또는 ⚪ 참고 |

> T1·T2가 있으면 T3·T4만 단독으로 🟢 인용 금지.
> 조세심판원 결정례(`sourceType='심판례'`)는 T3 (회계사 결정 2026-05-22, SSOT §7.6·PRD §6.1과 정합).

**시점 라벨 의무 (모든 답변 부착):**

- `[현행]` — 답변 생성 시점 시행 중 (법령)
- `[적용 시점: YYYY.MM.DD ~ YYYY.MM.DD]` — 회계사가 명시한 시점
- `[폐지: YYYY.MM.DD]` — 폐지된 조문 (⚫ 라벨 동반)
- `[결정: YYYY.MM.DD]` — 비법령(심판례·해석례·판례) 결정·선고·회신일 (TAX-037)

> 비법령에서 결정일 불명이거나 상시 해석 원칙인 경우 `[현행]` 사용 가능.
> 회계사 질문이 모호하면(`예전`, `이전 법`) **자의적 판단 금지** — 시점 확인 요청.

### 6.3 라벨링 시스템

| 라벨 | 의미 | 표현 |
|---|---|---|
| 🟢 직접 근거 | 검색된 조문이 직접 적용 | 단정형 허용 |
| 🟡 유사 사례 | 논리적 유사, 사실관계 차이 가능 | 단정 금지 ("유사 사례에서는 …") |
| ⚪ 참고 자료 | 관련 쟁점 다룸 | "참고가 될 수 있는 자료" |
| ⚫ 폐지 | 폐지·삭제된 조문 | 폐지 시점·최종 시행본 명시 |

- ❌ 검색 결과(`TaxLaw[]`)에 **없는 조문·예규·판례 인용 금지** (환각)
- ❌ 유사 사례 논리를 **직접 적용해 단정 금지** (메모리 `feedback_similar_cases.md`)
- ✅ 빈약 시 "직접 근거를 찾지 못했습니다" 명시 + 유사 사례 라벨링 제시

### 6.4 law-verifier 검증 V1~V6 (검증 우회 금지)

| 항목 | 통과 조건 | 실패 시 |
|---|---|---|
| **V1. 출처 존재** | 인용된 모든 항목이 검색 결과에 존재 (법령=조문번호 / 비법령=`caseNumber`로 대조) | 재검색 1회 → FAIL → E-VERIFY-FAIL |
| **V2. 인용 무결성** | 모든 발췌가 원문과 문자 단위 일치 | 재생성 1회 → FAIL → E-VERIFY-FAIL |
| **V3. 라벨 적정성** | Trust Tier에 맞는 라벨 사용 | 재생성 1회 |
| **V4. 시점 표기** | §6.2의 시점 라벨 부착 | 재생성 1회 |
| **V5. 면책 고지** | 답변 하단에 면책 부착 | 자동 부착 |
| **V6. 단정 금지** | 🟡에서 "이 케이스도 X입니다" 검출 | 재생성 1회 |

> 재시도 후에도 FAIL이면 **미검증 답변을 회계사에 노출하지 말 것**. 대신 "확인 어려움" 안내.
> 참고 목록(`LabeledAnswer.references`, 발췌 없는 비법령)은 **V1~V6 비대상·인용 승격 금지** (SSOT §7.4·PRD §6.5.3).

---

## 7. 개인정보·시크릿

- ❌ 검색어에 주민번호·사업자번호 포함 시 **입력 거부** (저장·전송 금지)
- ❌ API 키·토큰을 로그·에러 메시지·UI에 출력
- ❌ 회계사 식별자(이메일·이름·IP)를 로그에 포함
- ❌ `.env`, `.env.local` git 커밋
- ✅ 휴대폰·이메일은 마스킹 후 저장
- ✅ Vercel 환경변수(프로덕션) / `.env.local`(로컬) + `.env.example` 템플릿

### 7.1 환경변수 목록 (현행 필수)

| 환경변수 | 용도 | 추가 단계 |
|---|---|---|
| `NATIONAL_TAX_API_KEY` | 국세법령정보시스템 API | M1 |
| `OPENAI_API_KEY` | GPT-4o-mini (쿼리 변환·답변 생성) + text-embedding-3-small (임베딩) | M2 (TAX-011-B) |
| `DATABASE_URL` | pgvector 벡터 DB (Postgres + pgvector, Neon·Supabase·Vercel Postgres) | M4 (TAX-026-B) |

---

## 8. 개발 워크플로우

### 8.1 모든 작업은 티켓에서 시작

1. `docs/tickets/TAX-XXX_description.md` 작성
2. AI에게 "이 티켓 구현해줘" 지시
3. AI는 SSOT + PRD + CLAUDE.md + 티켓 + 관련 메모리 읽고 **계획서 제시**
4. 인간(회계사) **승인**
5. AI 구현
6. AI 검증 (M3 이후 골든셋 회귀 포함)
7. AI가 `docs/reports/TAX-XXX_report.md` **리포트 작성**

### 8.2 1 티켓 = 1 브랜치 = 1 PR

- 여러 티켓을 하나의 PR에 섞지 말 것
- 티켓 범위 밖 파일 수정 금지

---

## 9. AI 행동 10계명 (v2.0)

**[정확성]**
1. **인용 무결성** — 발췌 텍스트 의역 금지, 원문과 문자 단위 일치 (§6.1)
2. **원문 보존** — 법령 원문을 변형·요약 저장 금지
3. **검증 의무화** — law-verifier V1~V6 통과 없이 답변 노출 금지 (§6.4)
4. **시점 라벨** — 모든 답변에 `[현행]`/`[적용 시점]`/`[폐지]`/`[결정]` 부착 (§6.2)

**[범위]**

5. **SSOT 우선** — `docs/SSOT.md`와 충돌하는 규칙 만들지 말 것
6. **범위 엄수** — 티켓에 명시된 파일만 수정
7. **최소 변경** — 리팩터·폴더 구조 변경 금지 (명시 요청 제외)

**[절차]**

8. **계획 먼저** — 코딩 전 근본 원인·영향 파일·구현 계획 제시 후 인간 승인.
   - **승인 요청 시 선택지가 2개 이상이면 각 방안의 장점·단점을 한 줄씩 쉽고 명확하게 설명할 것** (비유·표 활용 권장, 추천안은 첫 번째에 배치)
   - 회계사 결정 부담을 줄이기 위함 — 참조: 메모리 `feedback_explain_before_decision.md`
9. **리포트 작성** — 구현 후 `docs/reports/`에 리포트 필수
10. **불확실하면 STOP & ASK** — 추측·임의 판단 금지, 반드시 질문

---

## 10. 구현 후 보고 형식 (필수)

```
### 변경 사항 요약

**파일 변경 목록:**
- src/adapters/xxx.ts (신규)
- src/usecases/xxx.ts (수정)

**주요 변경:**
- [기능 설명]

**검증 결과:**
1. npm run dev — 정상 작동
2. [기능별 검증 1~3건]
3. law-verifier V1~V6 통과 확인 (M3 이후)
4. 골든셋 회귀 합격선 통과 (M3 이후)

**잠재 위험:**
- [예상 위험요소]

**리포트:** docs/reports/TAX-XXX_report.md
```

---

## 11. 막힐 때 체크리스트

- 규칙이 모호한가? → `docs/SSOT.md` 재확인 (§7 세법 도메인 규칙)
- 제품 사양이 모호한가? → `docs/PRD.md` 재확인 (§6 정확성 보증 모델)
- 티켓이 애매한가? → **인간에게 질문**
- 세법 해석이 불확실한가? → **절대 임의 판단 금지**, 반드시 질문
- 외부 API 응답이 예상과 다른가? → 응답 샘플과 함께 인간에게 보고
- 인용 발췌가 원문과 일치하는가? → 정규화 후 문자 단위 비교 (§6.1)
- 시점 라벨을 부착했는가? → 모든 답변 필수 (§6.2)
- 회계사 승인이 필요한가? → 선택지 2개 이상이면 각 방안 장단점 한 줄씩 설명 (§9 8번)

---

## 12. 최종 원칙

> **"빠르게보다 안전하게, 똑똑하게보다 명확하게."**
>
> **"정확성 > 완전성 > 속도 > 편의성"**
>
> 회계사가 의뢰인의 의사결정에 사용합니다.
> **틀린 답은 없는 답보다 나쁩니다.**

---

**참조:**
- `docs/SSOT.md` — 코드·아키텍처 강제 규칙 (헌법)
- `docs/PRD.md` — 제품 사양·기능·KPI (사양서)
- `docs/tickets/_TEMPLATE.md` — 티켓 양식
- `.claude/agents/law-verifier.md` — 검증 서브에이전트 (TAX-003에서 작성)
- `eval/` — 골든셋 G-1~G-5 (M3 이후)
