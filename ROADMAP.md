# ROADMAP — 세법 API 검색 시스템 개발 로드맵

> 본 문서는 PRD §16(마일스톤)과 SSOT §11(우선순위 매트릭스)를 기반으로
> **언제 무엇을 어떤 순서로 만들 것인가**를 정의한 단일 로드맵입니다.
>
> 문서 위계: `docs/SSOT.md`(헌법) > `docs/PRD.md`(사양서) > `CLAUDE.md`(행동 요약) > **`ROADMAP.md`(로드맵)** > 티켓
>
> 충돌 시 상위 문서가 우선합니다. 본 로드맵의 모든 결정은 PRD/SSOT v2.0과 정합해야 합니다.
>
> 작성일: 2026-05-13 (v1.0) / 갱신: 2026-06-16 (v2.4 — Phase 6A·6B 전체 완결 + Vercel 베타 배포 완료 + PR #1 머지) / 2026-06-16 (v2.5 — Phase 7 운영 데이터 환류 정식 단계 신설) / 2026-06-21 (v2.6 — voyage-4 문서 정합 + TAX-6B-18A 심판례 수집기 추적) / 2026-07-06 (v2.7 — Phase 7 완료 반영 + 비법령 코퍼스 완전 확장·인용 연결망·검색 정확도 시리즈 반영)
> 버전: 2.7
> 작성자: Claude + 회계사

---

## 0. 핵심 원칙 (가장 먼저 읽을 것)

```
정확성(Accuracy)  >  완전성(Completeness)  >  속도(Speed)  >  편의성(UX)
```

> **"틀린 답은 없는 답보다 나쁘다."** — `CLAUDE.md` §12, `PRD.md` §0, `SSOT.md` §0

본 로드맵의 모든 단계 배치·우선순위·합격선은 위 원칙에 종속됩니다. 회계사가 의뢰인의 세무 의사결정에 사용하므로, 환각·의역·시점 오류 1건은 가산세·법적 분쟁으로 직결됩니다.

**Phase 3(law-verifier 통합) 통과 전 회계사 노출 절대 금지 원칙**
- M3 완료(law-verifier V1~V6 통과) 이전에는 LLM 생성 답변을 회계사에게 노출하지 않습니다.
- M2까지의 답변은 내부 검증·골든셋 작성용으로만 사용합니다.
- 검증 실패한 답변은 미검증 상태로 회계사에 노출 금지 (PRD §6.5.2, SSOT §6).

---

## 1. 프로젝트 개요

### 1.1 한 줄 정의

**국세법령·지방세법령·판례·예규를 자연어로 질문하면 LLM이 검색·정리·검증하여 인용 무결성과 시점 정확성을 보장한 답변을 회계사에게 전달하는 RAG 기반 세법 검색 어시스턴트.**

### 1.2 사용자

- **1차 사용자**: 회계사·세무사 (50명 이내)
- **용도**: 의뢰인 의사결정 보조 (사실관계 매칭 및 최종 적용 판단은 회계사 책임 — PRD §3.2)
- **개발 인력**: 회계사 1인 + AI 협업

### 1.3 기술 스택 (PRD §8 확정)

| 영역 | 선택 |
|---|---|
| Frontend | Next.js (App Router) + TypeScript + Tailwind CSS |
| RAG 프레임워크 | Vercel AI SDK (LangChain 미사용) |
| LLM | GPT-4o-mini (실증·운영) |
| 벡터 DB (M4+) | pgvector (Postgres) 또는 Pinecone |
| 임베딩 (M4+) | voyage-4 (1024차원, TAX-6B-15 이후 운영 기준) |
| 외부 API | 국세법령정보시스템, 지방세법령정보시스템 |
| 검증 | Claude Code 서브에이전트 (`.claude/agents/law-verifier.md`) |
| 배포 | Vercel |

### 1.4 RAG 5단계 파이프라인 (압축·생략 금지 — SSOT §3.3)

```
[1] 자연어 쿼리 변환 (LLM 1차)
       ↓
[2] 외부 API 검색 (국세·지방세 + 향후 벡터 DB)
       ↓
[3] 답변 생성 + 라벨링 + Trust Tier 적용 (LLM 2차)
       ↓
[4] law-verifier 검증 V1~V6 (필수, 우회 금지)
       ↓
[5] 회계사 화면 출력
```

---

## 2. 개발 단계 (Phases)

PRD §16의 M0~M6 마일스톤을 Phase로 정리합니다. 각 Phase는 상위 마일스톤의 산출물·티켓·검증 기준·상태를 포함합니다.

> 상태 범례: ✅ 완료 / 🟡 진행 중 / ⬜ 대기

---

### Phase 0 — M0: 프로젝트 기반 구축 ✅

**상태:** ✅ 완료 (2026-05-12)

**예상 기간:** 완료 (실측 — 하니스 엔지니어링 포함)
**예상 월 비용:** $0 (외부 호출 없음)

**목표:** Next.js 초기화, 폴더 구조 확정, 환경변수 템플릿, 문서 골격, AI 협업 하니스 완성.

**관련 티켓:** TAX-000 ~ TAX-009 (총 9건, 하니스 엔지니어링 완료)

**주요 산출물:**
- [x] Next.js (App Router) + TypeScript + Tailwind CSS 프로젝트 초기화
- [x] 폴더 구조 확정 (`app/`, `src/usecases/`, `src/adapters/`, `src/ports/`, `src/domain/`)
- [x] `.env.example` 템플릿 및 `src/config.ts` Fail-fast 패턴
- [x] `docs/SSOT.md` v2.0, `docs/PRD.md` v2.0, `CLAUDE.md` v2.0 작성
- [x] 티켓 양식(`docs/tickets/_TEMPLATE.md`) 및 리포트 양식 정착
- [x] AI 협업 가이드라인 및 메모리 체계 (`MEMORY.md`)
- [x] `README.md` 작성
- [x] 하니스 엔지니어링 완료 (TAX-001~009)

**검증 기준:**
- [x] `npm run dev` 정상 작동
- [x] 환경변수 누락 시 Fail-fast 동작
- [x] 폴더 구조가 SSOT §2와 일치

**비고:** 본 단계에서는 외부 API·LLM 연동을 포함하지 않으며, **답변 생성 경로 없음 → 회계사 노출 없음**.

---

### Phase 1 — M1: 국세법령 API 직접 매칭 검색 + 테스트 인프라 도입 ✅

**상태:** ✅ 완료 (2026-05-15)

**예상 기간:** 완료 (실측 2026-05-13 ~ 2026-05-15)
**예상 월 비용:** $0~5 (외부 API만 사용, LLM 미사용)

**목표:** 국세법령 API Adapter 구현 및 Port 정의, 직접 매칭 검색 API Route 제공. 출처·개정일·시행일·원문 링크를 포함한 검색 결과 스키마 확정. **본 Phase에서 Vitest + CI 테스트 인프라를 도입 의무화** (SSOT §2, PRD §15.0).

**관련 티켓:** 
- TAX-010 (가칭): 국세법령 API Adapter — PRD §16 기준 TAX-001 후속
- TAX-010a (가칭, 진입 직전 또는 병행): 테스트 인프라 도입 (Vitest + GitHub Actions CI)

**주요 산출물 — 코드:**
- [x] `src/ports/taxLawSearchPort.ts` — 검색 포트 인터페이스 정의
- [x] `src/adapters/nationalTaxLaw.ts` — 국세법령정보시스템 API 어댑터
- [x] `src/domain/TaxLaw.ts` — 도메인 엔티티 (법령명·조문·개정일·시행일·원문 링크·Trust Tier 필드 포함)
- [x] `src/usecases/searchTaxLaw.ts` — 직접 매칭 검색 Usecase 초기 버전
- [x] `app/api/search/route.ts` — 검색 API Route
- [x] 환경변수 `NATIONAL_TAX_API_KEY` 등록 (4곳 동시 갱신 — SSOT §4.1)
- [x] 검색 결과 정렬 기준 고정 구현 (개정일↓ → 시행일↓ → 조문번호↑ — SSOT §7.7)
- [x] 캐시 TTL 24시간 적용 (SSOT §7.3)

**주요 산출물 — 테스트 인프라 (v1.1 신설, PRD §15.0 정합):**
- [x] **Vitest 설치 및 설정** — `vitest.config.ts`, `package.json` 스크립트(`test`, `test:watch`, `test:coverage`)
- [x] **`tests/unit/` 디렉토리 신설** — 도메인 엔티티·정렬·캐시 키 등 순수 함수 단위 테스트
- [x] **`tests/integration/` 디렉토리 신설** — Adapter ↔ 외부 API 모킹(MSW 또는 vitest 내장) 통합 테스트
- [x] **PII 필터 단위 테스트** — 주민·사업자번호 패턴 입력 시 E-PII-DETECTED 거부 검증 (PRD §14.2)
- [x] **`.github/workflows/ci.yml`** — 린트·타입체크·Vitest 자동 실행 (PR 게이팅)
- [x] `npm run lint`, `npm run typecheck`, `npm run test` 스크립트 정착

**검증 기준:**
- [x] "부가가치세" 검색 → 10건 이상 결과 반환
- [x] 모든 결과에 출처·법령명·조문번호·개정일·시행일·원문 링크 포함
- [x] Fail-fast: `NATIONAL_TAX_API_KEY` 누락 시 앱 시작 실패
- [x] 동일 쿼리 재실행 시 동일 결과 (정렬 결정론성)
- [x] **단위 테스트 그린** (도메인·정렬·정규화 함수) — CI에서 자동 검증
- [x] **통합 테스트 그린** — 국세법령 API 모킹 시 정상 응답·에러 분기(E-API-TIMEOUT 등) 모두 통과
- [x] **PII 필터 단위 테스트 100% 통과** — 주민/사업자번호 패턴 입력 시 입력 거부 동작

**의존성:** Phase 0 완료 (충족). **관련 티켓:** TAX-010 계열

**회계사 노출 여부:** ❌ 아직 노출 금지 (LLM 답변 생성 경로 없음).

---

### Phase 2 — M2: RAG 통합 (자연어 쿼리 변환 + 답변 생성 + 라벨링) ✅

**상태:** ✅ 완료 (2026-05-17) — LLM: GPT-4o-mini (Gemini → OpenAI 전환 포함)

**예상 기간:** 완료 (실측 2026-05-15 ~ 2026-05-17)
**예상 월 비용:** $5~20 (GPT-4o-mini, PRD §17.3.1 상한)

**목표:** RAG 5단계 중 [1] 자연어 쿼리 변환, [3] 답변 생성 + 라벨링 + Trust Tier, [5] UI 출력 통합. 인용 복사 기능과 법적 면책 고지 자동 부착. **본 Phase 끝에 응답시간 P95 측정·기록 의무화** (PRD §7.1, §15.2 정합).

**관련 티켓:** TAX-011 (가칭) — PRD §16 기준 TAX-002

**주요 산출물:**
- [x] `src/adapters/llmQueryRewriter.ts` — 자연어 → SearchQuery 변환 (LLM 1차)
- [x] `src/adapters/llmAnswerGenerator.ts` — TaxLaw[] → LabeledAnswer 생성 (LLM 2차)
- [x] `src/domain/LabeledAnswer.ts` — 라벨링(🟢/🟡/⚪/⚫) + Trust Tier(T1~T4) + 시점 라벨 + 인용 발췌 + 면책 고지
- [x] `src/domain/Citation.ts` — 인용 단위 (원문·발췌·출처·Tier·시점)
- [x] `src/domain/TemporalContext.ts` — 시점 단서 추출 결과
- [x] `src/domain/disclaimer.ts` — 법적 면책 고지 상수 (SSOT §14.1)
- [x] LLM 시스템 프롬프트에 Trust Tier·라벨링 규칙·인용 무결성 규칙 강제 주입
- [x] LLM `temperature` 0.0~0.2, `top_p` 0.9 이하 (SSOT §7.7)
- [x] UI: 답변 화면 + 라벨 표시 + 원문 링크 + 시점 라벨 + 면책 고지 (FR-7, FR-16)
- [x] 인용 복사 기능 구현 (FR-14, PRD §11.2)
- [x] 시점 단서 모호 시 회계사 확인 요청 (E-TEMPORAL-AMBIGUOUS — PRD §13.1)
- [x] **응답시간 측정 계측 추가** (v1.1 신설) — RAG 누적 지연(쿼리 변환 + 검색 + 답변 생성)을 서버 로그에 기록

**검증 기준:**
- [x] 자연어 질문 → 라벨링된 답변 출력 (E2E 성공)
- [x] 모든 답변에 시점 라벨([현행]/[적용 시점]/[폐지])과 면책 고지 자동 부착
- [x] 인용 복사 시 발췌·출처·시점·면책이 일괄 클립보드 저장
- [x] Trust Tier 정렬 (T1·T2 우선 노출)
- [ ] **응답시간 P95 < 15초 사전 측정** (v1.1 신설) — Phase 2 후반 100회 측정, 미달 시 Phase 3 진입 전 재튜닝 `(측정 완료 — E2E 1회 약 16초, 목표 미달 · Phase 3 진입 전 재측정 필요)`
- [ ] **사전 환각률 측정** (v1.1 신설) — 임시 골든셋 5~10건으로 환각 발생 빈도 사전 확인 (M3 합격선 0% 달성 가능성 사전 검증) `(E2E 1회 파이프라인 정상 확인 · 공식 골든셋 5~10건 측정 미완)`
- [x] 단위·통합 테스트 그린 유지 (M1에서 도입된 인프라 사용)

**의존성:** Phase 1 완료.

**회계사 노출 여부:** ❌ **여전히 노출 금지** — law-verifier 통합(M3) 전까지 환각·인용 무결성 검증이 없으므로 회계사에게 답변 노출 불가. 내부 검증 및 골든셋 작성용으로만 사용.

---

### Phase 3 — M3: law-verifier 검증 레이어 통합 (V1~V6) + Playwright E2E 도입 ✅

**상태:** ✅ 엔지니어링 완료 (2026-05-17) — 잔여: 회계사 골든셋 30건 작성 + P95 재측정

**예상 기간:** **5~7주** (회계사 골든셋 30건 작성 시간 포함)
**예상 월 비용:** $20~50 (PRD §17.3.1 상한)

**목표:** law-verifier 서브에이전트 정의 및 검증 어댑터 구현. RAG [4]단계로 통합. **골든셋 G-1 30건** (SSOT §13.1, PRD §15.1.1 v2.1 하향) 작성 및 회귀 테스트 자동화. **본 Phase에서 Playwright UI E2E 도입 의무화** (SSOT §2, PRD §15.0). 본 단계 통과 시점부터 회계사에게 답변 노출 가능.

**관련 티켓:** 
- TAX-012 (가칭): law-verifier 통합 — PRD §16 기준 TAX-003
- TAX-012a (가칭, 병행): Playwright UI E2E 인프라 도입

**주요 산출물 — 검증 레이어:**
- [x] `.claude/agents/law-verifier.md` — 검증 서브에이전트 정의 (V1~V6 체크리스트 실제 구현과 정합)
- [x] `src/adapters/lawVerifier.ts` — 검증 어댑터 (V1~V6 규칙 기반, LLM 없음)
- [x] `src/domain/VerificationResult.ts` — 검증 결과 (PASS/FAIL/PENDING + 위반 항목)
- [x] `src/usecases/generateAnswer.ts` — 5단계 파이프라인에 [4] 검증 단계 통합
- [x] 재시도 정책 구현 (V1: 재검색 1회 / V2~V6: 재생성 1회 — PRD §13.2)
- [x] E-VERIFY-FAIL 처리: 미검증 답변 노출 금지 + "확인 어려움" 안내
- [x] UI PENDING 차단: AnswerCard에서 검증 전 답변 완전 차단
- [x] data-testid 속성 추가 (label-badge, temporal-label, disclaimer, pending-warning, verify-fail-message)

**주요 산출물 — 골든셋·회귀 테스트:**
- [x] `eval/golden_direct.json` — **검증 픽스처 10건** (PASS 6 + 네거티브 4: G-N1 환각/G-N2 의역/G-N3 잘못된 Tier/G-N4 시점 — BUG-002·006). 실세법 30건은 회계사 작성 잔여
- [x] `eval/README.md` — 골든셋 작성 가이드 및 합격선 명시 (※ "6건 100% PASS" 표현 갱신 잔여 — SSOT §9.3)
- [x] `tests/golden/run_golden.test.ts` — 골든셋 테스트 러너 (10건: PASS 6 + 네거티브 FAIL 4)

**주요 산출물 — UI E2E 인프라 (v1.1 신설, PRD §15.0 정합):**
- [x] **Playwright 설치 및 설정** — `playwright.config.ts`, `package.json` 스크립트(`test:e2e`, `test:e2e:ui`)
- [x] **`tests/e2e/` 디렉토리 신설**
- [x] **E2E 골든 시나리오 5건 작성:**
  - [x] PASS 답변 렌더링 (🟢라벨·[현행]·면책고지)
  - [x] E-VERIFY-FAIL "확인 어려움" 안내 정상 표시
  - [x] PENDING 상태 차단 (인용·요약 미노출)
  - [x] 로딩 상태 UX (버튼 비활성화 후 답변 표시)
  - [x] PII 감지 오류 메시지 표시
  - [ ] 인용 복사 클립보드 일치 — 잔여 (골든셋 30건 완성 후 추가 권장)
- [x] `.github/workflows/ci.yml`에 Playwright 통합 (check job → e2e job 순서 게이팅)

**검증 항목 (V1~V6 — SSOT §7.4):**
- [x] V1 출처 존재: 인용 조문이 검색 결과에 존재
- [x] V2 인용 무결성: 발췌가 원문과 문자 단위 일치
- [x] V3 라벨 적정성: Trust Tier에 맞는 라벨 (T1/T2→🟢⚫, T3/T4→🟡⚪⚫)
- [x] V4 시점 표기: 시점 라벨 부착
- [x] V5 면책 고지: 답변 하단 면책 자동 부착
- [x] V6 단정 금지: 🟡에서 단정 표현 검출

**검증 기준 (PRD §17.1, SSOT §13.3 합격선):**
- [ ] G-1 환각률 0% — **잔여: 회계사 골든셋 30건 + 실제 API 측정 필요** (네거티브 G-N1~N4가 환각·의역·Tier·시점 회귀를 코드 차원에서 선검증)
- [x] 인용 무결성 위반율 0% (golden_direct PASS 픽스처 6건 기준)
- [x] 라벨 정확도 ≥ 95% (golden_direct PASS 픽스처 6건 기준)
- [x] 시점 정확도 ≥ 95% (golden_direct PASS 픽스처 6건 기준)
- [ ] 재현율(Recall) ≥ 80% — **잔여: 실제 API 연동 후 측정 필요**
- [x] **UI E2E 골든 시나리오 5건 그린** (5/5 PASS)
- [ ] **응답시간 P95 < 15초 유지** — **잔여: 검증 단계 추가 후 재측정 필요**

**의존성:** Phase 2 완료.

**회계사 노출 여부:** ✅ **본 단계 PASS 시점부터 회계사 노출 가능**. 단, 검증 FAIL 답변은 절대 노출 금지.

---

### Phase 4 — M4: 벡터 DB + 의미 유사도 검색 ✅

**상태:** ✅ 완료 (2026-06-10) — TAX-026-B~H 전체 완결 (코드 구현 + Neon 실연결 + 임베딩 38건 적재 + 스모크 PASS)

**실측 기간:** 완료 (2026-06-10)
**실측 월 비용:** 임베딩 초기 적재 1회성 (이후 운영 비용 모니터링)

**목표:** 벡터 DB(pgvector) 구축 및 임베딩 모델 연동. 직접 매칭 빈약 케이스에서 유사 사례를 의미 유사도로 발굴 (FR-9). 3단계 fallback 검색 전략 활성화 (PRD §9.3).

**관련 티켓:** TAX-026-A~H (설계·코드·실연결 전체)

**주요 산출물:**
- [x] `src/ports/vectorSearchPort.ts`, `src/ports/embeddingPort.ts` — 포트 인터페이스 신설
- [x] `src/adapters/vectorSearch.ts` — PgVectorSearchAdapter (cosine 유사도, ssl 호환)
- [x] `src/adapters/embedding.ts` — OpenAIEmbeddingAdapter (text-embedding-3-small, 1536차원)
- [x] `src/usecases/searchWithFallback.ts` — FallbackSearchPort (direct→vector→expanded, THRESHOLD=3, TOP_K=10)
- [x] `downgradeVectorLabels` 후처리 — vector→🟡 천장, expanded→⚪ 천장 (TAX-026-G)
- [x] Neon pgvector 실연결 + `taxlaw_embeddings` 테이블 마이그레이션 완료
- [x] `DATABASE_URL` 환경변수 4곳 동시 갱신 완료 (SSOT §4.1)
- [x] `scripts/embed.ts` — 오프라인 임베딩 배치 (38건 전량 적재, content_hash 중복 방지, 재실행 안전)
- [x] `scripts/smokeVector.ts` — 운영 벡터 fallback 스모크 테스트 (`npm run smoke:vector`)
- [x] `.env.example` `DATABASE_URL=` 주석 해제 완료

**검증 기준:**
- [x] 직접 매칭 빈약 케이스에서 벡터 fallback 발동 (`matchStage=vector` 스모크 PASS 확정)
- [x] 유사 사례에 🟡 라벨 자동 적용 (`downgradeVectorLabels`, V3 정합)
- [x] G-1 회귀 테스트 합격선 유지 (vitest 387/387 PASS, 라벨 정확도 ≥ 95%)
- [ ] UI E2E 회귀 0건 — 잔여 (Phase 5 진입 전 권장)
- [x] 단위·통합 테스트 그린 유지 (vitest 387/387)

**잠재 위험 (후속 모니터링):**
- P95 answer tail latency (100회 17.76s) — `measureP95.ts`가 벡터 미경유(NationalTaxLawAdapter 직접 주입)이므로 벡터와 무관 확정. 병목은 gpt-4o-mini 긴 content 생성 지연.
- 임베딩 적재 38건(골든셋 기반) — 운영 커버리지 확대 시 대량 적재 별도 필요 (Phase 5 연계)

**리포트:** `docs/reports/TAX-026-H_report.md`

**의존성:** Phase 3 ✅ 완료. **→ Phase 5 착수 가능.**

**Phase 5와의 관계:** 임베딩 스키마·적재 파이프라인(`scripts/embed.ts`)이 Phase 4에서 완성됨. Phase 5는 판례·예규 데이터를 동일 파이프라인으로 적재하는 단계.

---

### Phase 5 — M5: 판례·예규 데이터 적재 및 임베딩 ✅

**상태:** ✅ 완료 (2026-06-10) — TAX-052~055 전체 완결 + G-2 골든셋 30건 검수·머지

**실측 기간:** 완료 (2026-06-10, AI 1일 + 회계사 검수)
**실측 월 비용:** 임베딩 1회성 $1 미만 (text-embedding-3-small)

**목표:** 판례·예규·해석례·심판례 데이터 적재 및 임베딩. T3·T4 출처의 정상 활용. 골든셋 G-2 30건 작성 및 회귀.

**관련 티켓:** TAX-052~055 (계획 시 TAX-014 가칭 → shrimp 4티켓으로 분할)

**핵심 통찰 — "엔진은 이미 있다, 연료만 넣으면 된다":** Phase 4에서 벡터 검색 엔진(FallbackSearchPort·embed.ts·downgradeVectorLabels)이 비법령(caseNumber)까지 지원 완료. Phase 5 신규 코드는 수집 스크립트 1개(collectNonlaw.ts)뿐, 나머지는 데이터 공급·검증·골든셋.

**주요 산출물:**
- [x] `scripts/collectNonlaw.ts` — 비법령 수집 파이프라인 (TAX-052: 5세목 18키워드, 3중 필터, caseNumber dedup, content 원문 불변)
- [x] 비법령 89건 임베딩 적재 — DB 합계 127건(법령34+심판례86+해석례4+판례3) + embed.ts BATCH_SIZE 100→20 버그픽 (TAX-053)
- [x] 벡터 fallback 비법령 통합테스트 12건 — caseNumber 이중노출 방지·라벨 다운그레이드·references 트랙 (TAX-054)
- [x] 검색 결과에 판례·예규 포함 (Trust Tier T3/T4 라벨링, smokeVector NONLAW 3/3 PASS)
- [x] **G-2 유사 사례 골든셋 30건** — 시드 30건 → AI summary 초안 → 회계사 검수 → `eval/golden_direct.json` 머지(40→66건, 검수완료 표기) (TAX-055)
  - ※ 계획상 별도 파일(`golden_similar.json`)이었으나 기존 골든셋과 동일 러너·status를 재사용하도록 `golden_direct.json` 통합으로 변경

**검증 기준:**
- [x] 판례·예규가 검색 결과에 정상 포함 (matchStage=vector 스모크 3/3 PASS)
- [x] **G-2 라벨 정확도 30/30 = 100%** (합격선 ≥ 95% 충족)
- [x] G-1 회귀 합격선 유지 (vitest 437/437, golden:status 66건 V1~V6 전체 PASS·불일치 0건)
- [ ] UI E2E 회귀 0건 — 잔여 (Phase 6A 진입 전 권장, Phase 4부터 이월)

**리포트:** `docs/reports/TAX-052_report.md` ~ `TAX-055_report.md`

**의존성:** Phase 4 ✅ 완료. **→ Phase 6A 착수 가능.**

---

### Phase 6A — M6A: 지방세 통합 + 시점 검색 + G-3·G-4 골든셋 ✅

> **v1.1 분할:** 기존 Phase 6에 골든셋 50건(G-3·G-4·G-5)과 4개 기능이 몰려 있던 과부하를 6A·6B로 분산합니다 (PRD §16 v2.1 정합).

**상태:** ✅ 완료 (2026-06-15)

**실측 기간:** 완료 (2026-06-11 ~ 2026-06-15)
**예상 월 비용:** $50~120 (PRD §17.3.1 상한)

**목표:** 지방세법령 API 통합(FR-2)으로 검색 범위를 국세 + 지방세로 확장. 시점 검색(FR-15) 활성화. 골든셋 G-3·G-4 작성·회귀.

**관련 티켓:** TAX-015 (가칭) — PRD §16 기준 M6A

**주요 산출물:**
- [x] TAX-6A-1: 지방세법령 API 진단 — 국세 API(`law.go.kr`) 동일 접근 확인, `LOCAL_TAX_API_KEY` 불필요
- [x] TAX-6A-2·3: 취소 (지방세 별도 어댑터 불필요 — 기존 어댑터가 지방세 조문 포함)
- [x] TAX-6A-4~7: 시점 검색 로직·UI·G-3·G-4 골든셋 구현 (FR-15)
- [x] TAX-6A-8: False Premise Defense — SYSTEM_PROMPT 전제 검증 지침, G4-03·04·06 PASS
- [x] TAX-6A-9: G-3 법령 연혁 API 방안 A 완결 (케이스 재설계, lawVerifier 단일날짜·비법령V2면제)
- [x] TAX-6A-10: G-3 V-FAIL 진단 + V3 안전망 양방향 확장
- [x] TAX-6A-11: 라벨 결정론화(처방 D, LLM 라벨 버리고 tier로 재계산) + temperature 고정(처방 F)
- [x] G-3 골든셋 9/20 PASS 확정 (11건은 구조적 검증 실패로 TAX-6A-10 이연)
- [x] G-4 환각 유발 골든셋 완료

**검증 기준:**
- [x] 시점 검색 정확도 G-3 9/20 PASS (구조적 한계 11건 이연)
- [x] 환각 방어 G-4 PASS
- [x] G-1·G-2 회귀 합격선 유지
- [x] 라벨 결정론화로 G-3 비결정성 완결 (2회 연속 11/11 PASS 확인 후)

**의존성:** Phase 5 완료.

---

### Phase 6B — M6B: 부칙·경과조치 + 편의 기능 + G-5 골든셋 ✅

**상태:** ✅ 완료 (2026-06-16)

**실측 기간:** 완료 (2026-06-14 ~ 2026-06-16)
**예상 월 비용:** $50~150 (PRD §17.3.1 상한)

**목표:** 부칙·경과조치 자동 연결(FR-17)로 신·구법 적용 경계 명시. 최근 검색어(FR-11)·즐겨찾기(FR-12) 편의 기능 통합. UI 다듬기 + 폐지 조문 동작 검증.

**관련 티켓:** TAX-016 (가칭) — PRD §16 기준 M6B

**주요 산출물:**
- [x] TAX-6B-1: 부칙·경과조치 동반 조회 (FR-17 백엔드) — 시점 경계 선별, T2 Trust Tier
- [x] TAX-6B-2: 부칙 답변·UI 표시 (FR-17 프론트엔드) — ⏱경과조치 배지, 인디고 보더
- [x] TAX-6B-3: 최근 검색어 (FR-11) — PII 마스킹(휴대폰·이메일), controlled input + 드롭다운
- [x] TAX-6B-4: 즐겨찾기·북마크 (FR-12) — localStorage CRUD + §7 마스킹
- [x] TAX-6B-5: UI 다듬기 — 라벨/Tier 배지 툴팁, 키보드 단축키(Ctrl+K·Escape), 접근성
- [x] TAX-6B-6: G-5 폐지 골든셋 골격 10건 + run_golden 로더 연결
- [x] TAX-6B-7: G-5 실측 + 회계사 검수 → expectedStatus 10건 전건 PASS 확정
- [x] TAX-6B-8: FR-18 골든셋 회귀 CI (`.github/workflows/golden.yml`) — master push·PR 트리거
- [x] `eval/golden_repealed.json` — G-5 폐지 조문 골든셋 10건 완성
- [x] vitest 545/545 GREEN, run_golden 105/105 GREEN

**검증 기준:**
- [x] 부칙·경과조치가 본 법령과 함께 검색 결과에 노출 (T2 라벨 정상)
- [x] 폐지 조문 ⚫ 라벨 정상 동작 (G-5 10건 PASS)
- [x] 최근 검색어 마스킹 동작 (PII 단위 테스트 통과)
- [x] G-1~G-5 전체 회귀 합격선 유지 (run_golden 105/105)
- [ ] **회계사 분기 설문 만족도 측정** (PRD §17.2) — 베타 운영 1분기 후

**핵심 발견 — V1~V6 한계:** V1~V6는 "거짓말(환각·오인용) 탐지기"이지 도메인 정확도 검증기가 아님.
G5-06(대체조항 누락)·G5-10(현행 조문 검색 누락) 같은 조용한 오류는 통과. → **TAX-6B-9 내용검증기(Phase 7 후 결정)**

**의존성:** Phase 6A 완료.

---

### Phase 7 — M7: 운영 데이터 환류 + 내용 검증기 + 비법령 정확도 ⬜

> **v2.5 신설.** 베타 배포(TAX-056) 이후 운영 단계 진입에 맞춰, 그동안 "향후 트랙(Off-roadmap)"으로만 있던
> 운영 환류(TAX-030)를 **정식 Phase로 승격**합니다. 회계사 결정 3건(2026-06-16)을 반영해 저장소·수집범위·내용검증기 방안을 확정했습니다.

**상태:** 🟡 부분 완료 (2026-07-06 기준) — TAX-030-A~C·TAX-6B-9 완료, TAX-044/045(운영 데이터 축적 후)만 잔여

**예상 기간:** 운영 단계 (수집 인프라 착수 → 데이터 1~2주 축적 대기선 → 정확도 개선)
**예상 월 비용:** $0 추가 (기존 `DATABASE_URL`·LLM/임베딩 키 재사용 — 신규 환경변수 없음)

**본질:** "수집 인프라(TAX-030-A)를 먼저 켜고, 데이터 쌓이는 1~2주 동안 데이터 비의존 작업(내용 검증기·신고 버튼)을 병행 → 데이터 모이면 TAX-044/045로 정확도 개선."

**회계사 결정 3건 (2026-06-16 확정):**
1. **저장소 = 기존 Neon Postgres 재사용** (파일 저장은 Vercel 서버리스에서 휘발 → TAX-030 원안 `logs/*.json` 폐기). `DATABASE_URL` 이미 연결(Phase 4). **신규 환경변수 없음.**
2. **수집 범위 = 성공 쿼리까지 전부** (E-VERIFY-FAIL만 아님). 사유: TAX-044 도메인 사전을 실제 검색 패턴 전수로 작성.
3. **내용 검증기 = 방안 A(규칙 기반 `expectedContent` 대조)** 로 Phase 7 포함. V1~V6와 완전 분리된 별도 레이어.

**관련 티켓 (구현 순서):**

| 순서 | 티켓 | 내용 | 데이터 의존 | 상태 |
|---|---|---|---|---|
| 1 | TAX-030-A | 자동 수집 훅 — `IOpsLogPort`+`opsLog`(Pg/Null, fail-soft)+`generateAnswer` 옵셔널 주입+`ops_query_log`/`ops_feedback` 마이그레이션 | 무 (임계 경로) | ✅ 완료 (vitest 576/576) |
| 2 | TAX-030-B | 조용한 틀림 신고(👎) — `app/api/feedback/route.ts`+AnswerCard 버튼+`recordFeedback` (선결=030-A) | 무 | ✅ 완료 (vitest 587/587, Neon 마이그레이션 완료) |
| 3 | TAX-6B-9 | 내용 검증기(방안 A) — `src/domain/contentVerify.ts`+골든셋 `expectedContent`(회계사 작성)+run_golden 확장 | 무 | ✅ 완료 (vitest 607/607) — `expectedContent` 키워드는 AI 제안, 회계사 검수 잔여 |
| 4 | TAX-030-C | 환류 스크립트 — `harvestToSeeds.ts`(DB→시드)→buildCases(TAX-028 재사용)→회계사 검수→골든 머지→CI 박제 (선결=030-A/B) | 무 | ✅ 완료 (vitest 595/595) — 정답 조문은 회계사 수기 입력 필수 |
| — | (대기선) | 운영 1~2주 데이터 축적 | — | — |
| 5 | TAX-044 | 세무 도메인 사전 + 법리·사실축 결합 검색 (옵션 A) | **유 (1~2주)** | ⬜ 대기 |
| 6 | TAX-045 | 저정보 조문 패널티 + 사후 결과 검증 (선결=TAX-044) | 유 | ⬜ 대기 |

**연계 FR:** FR-23(운영 쿼리 로그 자동 수집)·FR-24(조용한 틀림 신고)·FR-25(내용 검증기) — PRD §5.2 v2.5 신설. FR-18(골든셋 회귀 자동화)의 종착.

**불변 제약 (CLAUDE.md §2·§6·§7, SSOT §7.8·§13.2):**
- ❌ 정답(`summary`·`expectedStatus`·`expectedContent`) 자동 생성 금지 — 회계사 검수 필수(자기참조 채점 방지).
- ❌ PII·회계사 식별자(이메일·이름·IP) 저장 금지 — 마스킹·익명화를 수집 1단계부터(식별자 컬럼 미존재).
- ❌ law-verifier V1~V6 검증 로직 무변경 — 내용 검증기는 별도 레이어.
- ❌ 검증 결과(`TaxLaw[]`) 데이터 변형 금지 (TAX-044/045는 입력 정규화·가중치만).
- ✅ 수집은 fail-soft — 로그 적재 실패가 답변 생성을 막지 않는다.

**검증 기준:**
- [ ] 운영 로그 마스킹·식별자 미저장 확인 (SSOT §7.8)
- [ ] 수집 fail-soft — DB 장애 시에도 답변 경로 무영향
- [ ] 내용 검증기 CI 골든셋 회귀 편입 (FR-18)
- [ ] 환류 시 회계사 검수 게이트 통과(정답 자동생성 금지)
- [ ] G-1~G-5 기존 회귀 합격선 유지

**의존성:** Phase 6B 완료 + law-verifier(Phase 3 ✅) + 골든셋 보조(TAX-028 ✅) + Neon(Phase 4 ✅) + 베타 배포(TAX-056 ✅).

> **하위 호환 메모:** 아래 두 "별도 트랙"(비법령 정확도 후속 개선·틀린 케이스 골든셋 환류)은 본 Phase 7로 통합·승격되었습니다. 두 트랙의 상세 설계 내용은 이력 보존을 위해 아래에 그대로 둡니다.

---

### 별도 트랙 — 비법령 직접검색 (Off-roadmap, M5 가치 일부 선제 달성) ✅

> **이 트랙은 PRD §16 마일스톤(M0~M6B)에 없던 별도 트랙입니다.** 벡터 DB(Phase 4/5) 진입 전,
> 회계사 실무 요청에 따라 **국세법령정보 OpenAPI를 직접 호출(임베딩·벡터 DB 없음)**해
> 비법령 자료(판례·해석례·심판례)를 검색 결과에 편입했습니다. M5(판례·예규 적재·임베딩)가 지향한
> "검색 결과에 판례·예규 포함" 가치의 **일부를 다른 방식으로 선제 달성**한 것입니다.

**상태:** ✅ 완결 (2026-05-20 ~ 2026-05-22). 엄브렐러 `TAX-016` 종료.

**예상 월 비용:** $0 추가 (기존 `NATIONAL_TAX_API_KEY` 재사용 — 새 환경변수 없음)

**목표:** 직접 매칭 검색에 비법령 자료 4종을 같은 `TaxLaw` 그릇("통합 서랍")으로 편입하여,
실무 판단 순서(조문 → 해석례 → 심판례 → 판례)를 시스템이 온전히 보조하도록 함.

**완료 자료원:**

| 티켓 | 자료 | API target | Trust Tier | 본문 | 노출 방식 |
|---|---|---|---|---|---|
| TAX-015 / 015B / 015C / 015D | 판례 | `prec` | T4 | 법원 출처만 있음 | 본문 O → 발췌 인용(citable·V1~V6) / 본문 X(국세 출처) → 참고 목록 |
| TAX-016A | 법령해석례 (법제처·기재부 회신 포함) | `expc` | T3 | 있음 | 발췌 인용(citable·V1~V6) |
| TAX-016B | 국세청 해석 | `ntsCgmExpc` | T3 | 없음(목록만) | 참고 목록(references·V검증 비대상) |
| TAX-016C | 조세심판원 결정례 | `ttSpecialDecc` | T3 | 있음 | 발췌 인용(citable·V1~V6) |

**주요 산출물:**
- [x] `src/domain/TaxLaw.ts` — `sourceType('법령'|'판례'|'해석례'|'심판례')` + 비법령 선택 필드(`caseNumber`·`issuingBody`·`decisionDate`) 추가 (하위호환 유지)
- [x] `src/adapters/nationalTaxLaw.ts` — 4종 target 병렬 검색·정규화·병합(법령 → 해석례 → 심판례 → 판례, 날짜↓, 부분 실패 허용). 본문 있는 자료는 목록→본문 2단계 조회
- [x] `src/domain/LabeledAnswer.ts` — `references?: TaxLaw[]` 신규. 본문 없는 비법령 + 인용 안 된 본문 있는 비법령을 관련도순 상위 N건(`MAX_REFERENCES=10`)으로 "참고 목록" 노출
- [x] `src/usecases/generateAnswer.ts` — `splitResults`(content 유무로 citable/references 분리) + `buildReferences`(관련도순 정렬)
- [x] `src/adapters/lawVerifier.ts` — V1 식별자 분기(법령=조문번호, 비법령=caseNumber). V2~V6 로직 불변. **references는 V검증 비대상(인용 승격 금지)**
- [x] `app/components/AnswerCard.tsx` — 자료유형 배지(법령/판례/해석례/심판례), `dateLabel`로 일자 문구 분기(선고일/회신일/해석일/결정일), 참고 목록 섹션
- [x] 원문 링크에서 API 키(OC) 제거 재구성 (판례 `precInfoP.do` / 해석례 `expcInfoP.do` / 심판례 `allDeccSc.do?query=청구번호`). 국세청 해석은 공개 뷰어라 키 미포함

**검증 기준:**
- [x] `npm run typecheck` / `lint` / `test` 그린 — **테스트 127개 통과**(101 → 127 누적)
- [x] 본문 있는 비법령(판례 법원출처·해석례·심판례) 인용에 V1~V6 정상 동작(자료유형별 환각/의역/Tier FAIL 케이스 단위 검증)
- [x] 참고 목록(references)은 발췌 없음·V검증 비대상(인용 무결성 충돌 회피)
- [x] 기존 법령 검색 회귀 없음, 원문 문자 단위 보존
- [ ] 회계사 수동 검증 — `npm run dev` → 조세 불복·해석 쟁점 검색 → 4종 노출·원문 링크 이동 확인 (잔여)

**Phase 4/5(벡터 DB)와의 관계:**
- **대체 아님, 보완·선행.** 본 트랙은 *직접 매칭* 기반 비법령 검색이고, Phase 4/5는 *의미 유사도(임베딩)* 기반 검색입니다.
- 벡터 DB 도입 시: 직접검색(정확한 키워드 일치) + 벡터검색(의미 유사) + 참고 목록(관련도순)이 **3단계 fallback 전략(PRD §9.3)** 안에서 보완 관계로 결합됩니다.
- 따라서 Phase 4/5는 본 트랙을 **재구현하지 않고**, 직접검색으로 못 잡는 빈약 케이스를 임베딩으로 메우는 데 집중합니다.

**잔여·후속 (문서 정합 — TAX-017로 완료):**
- ✅ **SSOT/PRD/CLAUDE.md §6.2 등급표에 "심판례 T3" 명문화 완료** (TAX-017 — SSOT v2.2 §7.6·PRD v2.2 §6.1·CLAUDE.md §6.2).
- ✅ **PRD 정합 완료** (TAX-017) — FR-19/20 신설·Trust Tier 표·V1 식별자 분기·references 규칙·§9.3 라벨 보정·§10 데이터 모델·§16 별도 트랙 행 반영.
- (보류) 골든셋 비법령 셋(G-N1~N4) PRD §15.1 편입 — 정답값 회계사 검수 후.

> 근거 리포트: `docs/reports/TAX-015_report.md` ~ `TAX-016C_report.md`, 엄브렐러 `docs/tickets/TAX-016_interpretation_tribunal_search.md`.

---

### 별도 트랙 — 비법령 검색 정확도 후속 개선 (운영 데이터 환류) 🟡

> **상위 트랙(비법령 직접검색)의 연속선.** 비법령 4트랙(판례·해석례·NTS해석·심판례)을 회계사 실무에 편입한 뒤,
> 같은 어댑터(`nationalTaxLaw.ts`) 위에서 **검색 입력 정규화 → 도메인 사전 → 사후 검증** 순으로 정확도를 단계적으로 끌어올리는 트랙.
> 진입은 본 시스템이 회계사 실사용에 들어간 후 (운영 1~2주 분량의 실제 검색 로그 표본 확보 후).

**상태:** 🟡 부분 진행 — 단계 1(TAX-043) 완료 (2026-06-08), 단계 2·3은 운영 데이터 대기

**예상 월 비용:** $0 추가 (기존 `NATIONAL_TAX_API_KEY`·LLM/임베딩 키 재사용 — 신규 환경변수 없음)

**진입 조건:**
- 본 시스템이 회계사 실사용 단계 진입 (Phase 3 노출 게이트 통과 후)
- **단계 2·3 한정**: 비법령 검색 로그 1~2주분 표본 확보 — 회계사가 실제로 던지는 쿼리 패턴 식별 후 도메인 사전을 데이터 기반으로 작성

**완료·계획 티켓:**

| 단계 | 티켓 | 옵션 | 효과 | 상태 |
|---|---|---|---|---|
| 단계 1 | TAX-043 — 비법령 검색 자연어 정규화 | 옵션 B + C | 자연어 군더더기 제거(불용어 29개) + 사건번호 정확매칭(판례·심판례) | ✅ 완료 (2026-06-08) |
| 단계 2 | TAX-044 (계획) — 세무 도메인 사전 + 법리·사실축 결합 검색 | 옵션 A | ⭐⭐⭐ 비법령 적중률 30~50% ↑ | ⬜ 운영 1~2주 후 |
| 단계 3 | TAX-045 (계획) — 저정보 조문 패널티 + 사후 결과 검증 | 옵션 D + E | ⭐⭐ 라벨 정확성 안정화 | ⬜ TAX-044 완료 후 |

**단계별 의존성 (중요 — 변수 분리 원칙):**
- **TAX-044는 TAX-043 운영 효과 측정 + 회계사 실제 검색 로그**가 있어야 도메인 사전을 정확히 만들 수 있다 (추측 기반 사전 방지). 동시 진행 시 어느 변경이 효과를 냈는지 분리 불가.
- **TAX-045는 TAX-044의 결합 검색이 안정된 후** 사후 검증을 얹어야 효과 측정이 명확해진다.

**불변 제약 (CLAUDE.md §2·§6.1·§6.4):**
- ❌ 검증 결과(`TaxLaw[]`) 데이터 변형 금지 — 입력만 정규화·가중치 조정
- ❌ V1~V6 검증 로직 무변경 — 검증 우회 금지
- ❌ 법령 트랙(`fetchArticles`) 무변경 — TAX-031 `normalizeLawName`과 충돌 회피

**근거 리포트:**
- `docs/reports/TAX-042E_nonlaw-search-insights.md` — 5개 옵션(A·B·C·D·E) 단계별 묶음 권고
- `docs/reports/TAX-043_report.md` — 단계 1(옵션 B+C) 구현·검증 결과

**상위 트랙(비법령 직접검색)과의 관계:**
- 상위 트랙은 **"무엇을 가져올까"**(4트랙 자료 편입 — TaxLaw 그릇 통합)
- 본 트랙은 **"어떻게 잘 찾을까"**(편입된 자료의 검색 정확도 향상 — 입력·검색·결과 3단계)
- 같은 `nationalTaxLaw.ts` 어댑터 위에서 단계적으로 누적

---

### 별도 트랙 — 비법령 코퍼스 완전 확장 (판례·심판례 pgvector 전량 적재) ✅

> **상위 트랙(비법령 직접검색)의 데이터 규모 확장.** OpenAPI 직접검색(TAX-015~016)이 "무엇을 가져올까"를 해결했다면,
> 본 트랙은 판례·심판례를 pgvector(Phase 4 인프라)에 **전량** 적재해 의미 유사도로도 발굴되도록 한다.

**상태:** ✅ 완결 (2026-06-17 ~ 2026-07-04)

**예상 월 비용:** $0 추가 (기존 `VOYAGE_API_KEY`·`DATABASE_URL` 재사용, 임베딩은 1회성 배치)

**완료 티켓:**

| 티켓 | 내용 | 상태 |
|---|---|---|
| TAX-6B-13 | 로컬 판례 코퍼스 pgvector 적재 PoC(300건) | ✅ 완료 |
| TAX-6B-14 | 판례 코퍼스 라이브 배선 — 참고 목록에 판례 의미검색 합류(보수적 2단 게이트: similarity≥0.5·상위 2건) | ✅ 완료 |
| TAX-6B-15 | 임베딩 모델 OpenAI(1536차원) → **voyage-4(1024차원)** 전환. 포트-어댑터 패턴으로 usecase 무변경, 롤백 경로 보존 | ✅ 완료 |
| TAX-6B-16 | 판례 코퍼스 전량 적재 — PoC 300건 → **10,083건**(대법원+하급심) | ✅ 완료 |
| TAX-6B-17 | `compactLawContent`·`SAFE_INPUT_TOKENS` 축소(60K→35K)로 generate 지연 단축 — G3 타임아웃 대응 | ✅ 완료 |
| TAX-6B-18 / 18A / 18B | 심판례 수집기(`collectTribunal.ts`) 구현 → **139,791건 전량**(voyage-4-large) 임베딩·적재(실 적재 135,810건, 3,981건은 병합사건 동일본문 정상 dedup) → 참고 목록 벡터 게이트에 판례와 병렬(Promise.all) 배선 | ✅ 완료 |
| TAX-6B-19 | 해석례(법제처 `expc`+국세청 `ntsCgmExpc`) 처리를 **목록 전용**으로 통일 — 국세청 본문 API 부재 확정(3중 실증), N+1 제거로 P95 개선(PRD FR-19/20 정합) | ✅ 완료 |

**핵심 트레이드오프 (회계사 승인 반영):**
- 심판례·판례 전량 적재의 P95·정확도 정량 회귀는 **생략**(LLM 과금 부담 우선순위 낮음 — "정확도 > 속도" 재확인)
- 심판례 벡터 질의 1건이 HNSW 인덱스 부재로 약 3.17초 소요하는 신규 위험 실측 — 전수 스캔 유지 결정(TAX-6B-18 리포트, 회계사 승인)
- 해석례는 본문 조회를 포기하는 대신 항상 참고 목록(🟡/⚪) 트랙 — 🟢 직접 근거 승격 불가(정확성 vs 속도 트레이드오프 명문화)

**검증:** vitest 694/694(6B-18 시점) → 이후 단계별로 누적 증가.

**리포트:** `docs/reports/TAX-6B-13_report.md`~`TAX-6B-19_report.md`

---

### 별도 트랙 — 판례·심판례 인용 연결망 (Citation Graph) ✅ (33은 대기)

> **"검색 결과가 직접 지목한 선례를 자동으로 함께 보여준다"** — 벡터 유사도로는 못 잡는 **원문 내 명시적 인용 관계**를
> 별도 그래프(Neon `citation_edges` 테이블)로 추출·적재하고, 참고 목록 확장·랭킹에 반영하는 트랙.

**상태:** ✅ 완결 (2026-06-26 ~ 2026-07-06), 단 **TAX-6B-33(OVERRULED 검수 큐)은 미착수**

**예상 월 비용:** $0 추가 (정규식 추출, LLM·임베딩 호출 0 — SQL 배치만)

**완료 티켓:**

| 티켓 | 내용 | 상태 |
|---|---|---|
| TAX-6B-23 | 판례 인용 연결망 PoC — 정규식만으로 내부 연결 밀도 64.3%(하한) 실측, 그래프 DB 도입 타당성 확인 | ✅ 완료 |
| TAX-6B-36 | 판례 수집기에 `참조판례`(구조화)·`판례내용`(전문) 인용 원천 필드 보존(추가 API 호출 0) | ✅ 완료 |
| TAX-6B-37 | 심판례 수집기에 참조결정 원천 재수집(`--citation-source`, 139,840건 전량, 참조결정 보유 27.3%) | ✅ 완료 |
| TAX-6B-31 | `citation_edges` 추출·적재 — 사건번호 0채움 정규화(`normalizeTribunalCaseNumber`)로 body 매칭 5.8%→81.3% 회복. **최종 DB 132,381행**(in_corpus 73.2%), 리딩케이스 조심2017서0991(189회 피인용) | ✅ 완료 |
| TAX-6B-32 | 참고 목록에 인용 그래프 반영 — 1-hop 확장(원문 직접 지목 선례 추가, 상한 3건) + 피인용수 부스트(log 스케일). 승격 금지 구조적 보장(확장 문서는 `references`만, `citations` 접근 지점 없음) | ✅ 완료 (라이브 검증 완료, §아래 참조) |
| TAX-6B-34 / 35 | 심판례·판례 **주간 증분 수집기**(`--incremental`) — 원장∪DB 이중 기지 판정, 판례는 JO(참조법령)+사건명 세목 이중 그물(하급심 사각지대 보완) | ✅ 완료 |

**대기 티켓:**
- **TAX-6B-33** (OVERRULED 검수 큐) — 판례·심판례 "뒤집힘(변경·폐기)" 자동 확정을 금지하고 회계사 수기 검수를 거치는 안전장치. 초안만 존재, 회계사 승인 대기.

**라이브 검증 (2026-07-06 완료):** 실질의("가지급금 인정이자 관련 심판례") 실행 결과, 벡터 검색 상위권에 없던 선례가 다른 참고문서 본문 내 직접 인용(`"(국심 92서1538, 1992.6.20 동일건)"`)을 근거로 참고 목록에 자동 추가됨을 확인. Trust Tier(T3🟡) 유지·원문 링크 정상·`citations` 미승격(구조 확인)·V1~V6 전부 PASS.

**검증:** vitest 768/768 GREEN(신규 13건 포함, 무회귀).

**리포트:** `docs/reports/TAX-6B-23_report.md`, `TAX-6B-31_report.md`, `TAX-6B-32_report.md`, `TAX-6B-36_report.md`, `TAX-6B-37_report.md`

---

### 별도 트랙 — 검색 정확도 지속 개선 시리즈 (P1~P7) ✅

> **운영 로그·회계사 실사용 관찰에서 발견된 검색 재현율·정렬 결함을 순차 수정한 트랙.**
> 각 항목은 좁은 범위의 근본원인 수정이며, 새 기능이 아니라 기존 검색 로직의 결함 제거.

**상태:** ✅ 완결 (2026-06-17 ~ 2026-07-02)

**예상 월 비용:** $0 추가 (검색 로직 조정만, 신규 외부 호출 없음)

**완료 티켓:**

| 티켓 | 문제 | 해결 | 상태 |
|---|---|---|---|
| TAX-6B-10 | 심판례 관련도 점수가 사건명만 봄 | 본문까지 매칭(제목 2점/본문 1점) + 불용어 제거 + 무관 시 빈 배열 컷오프 | ✅ 완료 |
| TAX-6B-11 | 참고 목록 후보군이 좁아 유실 발생 | 목록 조회 5→12건 확대, 본문 조회는 관련도 상위 5건만(N+1 억제) | ✅ 완료 |
| TAX-6B-12 | 표기 변이("양도소득세" vs "양도세")로 관련 자료 컷오프 탈락 | cosine 유사도 결합 점수로 의미상 구제, 임베딩 실패 시 글자 점수로 degrade | ✅ 완료 |
| TAX-6B-24 | 법리축+사실축 결합 검색어가 법령명 매칭을 무력화(재현율 0건 실측) | `splitLegalAxis`로 법리축만 분리해 법령 검색에 사용 | ✅ 완료 |
| TAX-6B-25 | 조문 관련도가 제목만 보고 본문 무시 → 쟁점 조문이 컷오프 탈락 | `nonLawRelevance.scoreRelevance` 재사용해 본문 반영 정렬 | ✅ 완료 |
| TAX-6B-26 | LLM이 생성한 다중 쿼리 중 1개만 검색·나머지 폐기 | 방안 A: 전량 병합 검색 + 벡터 fallback은 대표쿼리 1회(P95 보호) | ✅ 완료 |
| TAX-6B-27 | 쿼리 변환 프롬프트 "10자 이내"·"축 결합" 규칙 자기모순 | 모순 제거, 축 결합 우선 명시 | ✅ 완료 |
| TAX-6B-28 | 사문화된 라벨-값 프롬프트 규칙 잔존 | 죽은 규칙 삭제 + 코드 안전망(`ensureNoDirectBasisDisclosure`) 이중화 | ✅ 완료 |
| TAX-6B-29 | "접대비" 단독 키워드가 조문번호 힌트에 없어 법령 누락 | 법인세법 제25조 힌트에 키워드 추가(운영 로그 근거) | ✅ 완료 |
| TAX-6B-30 | 빈약 판정(THRESHOLD)이 "본문 보유"만 세고 관련성 무시 | `relevantContentCount`(본문+관련도>0)로 교체 | ✅ 완료 |

**검증:** vitest 610/610 → 673/673까지 순차 누적 GREEN(각 티켓 단위테스트 신규 포함, 무회귀). 정량 골든셋 재측정은 대부분 회계사 일회성 확인으로 대체(LLM 과금 최소화).

**리포트:** `docs/reports/TAX-6B-10_report.md`~`TAX-6B-30_report.md`

---

### 향후 트랙 — 틀린 케이스 → 골든셋 환류 (운영 단계, 계획) ⬜

> **운영 단계(Phase 4 이후) 진입 시를 위한 설계 트랙.** 즉시 구현이 아니라 계획 문서로 선반영한다.
> 골든셋이 "정확도를 지키고 약점을 드러내는" 도구라면, 본 트랙은 **실전 오류를 자동으로 모아주는 공급선**이다.

**상태:** ⬜ 계획 (티켓 초안 `docs/tickets/TAX-030_wrong_case_harvesting.md`)

**목표:** 운영 중 발생한 틀린 답변을 수집해 골든셋으로 환류(feedback loop)하여 같은 실수의 재발을 막는다.

**두 종류의 틀림 (핵심 구분):**

| 종류 | 무엇 | 수집 방식 | 위험도 |
|---|---|---|---|
| 시끄러운 틀림 | 검증이 스스로 FAIL 판정(`E-VERIFY-FAIL`) | 자동 수집 훅 | 낮음 (이미 차단됨) |
| **조용한 틀림** | 검증은 통과했으나 **실제 오답** | 회계사 "👎 신고" 버튼 | **높음** (자동 탐지 불가) |

> 조용한 틀림은 회계사가 의뢰인 보고서에 인용할 수 있어 가장 위험하며, **자동으로는 절대 못 모은다** →
> 사람 피드백(신고 버튼)이 유일한 수집 경로. "완전 자동 수집"은 이 절반을 놓친다.

**환류 흐름:**
```
수집(자동 FAIL + 사람 신고) → PII 마스킹·익명화
   → buildCases 골격화(TAX-028 재사용) → 회계사 검수(정답 확정)
   → golden_direct.json 머지 → CI 회귀 박제(FR-18) → 재발 시 PR 차단
```

**불변 제약 (CLAUDE.md §2·§7):**
- ❌ 정답(`summary`·`expectedStatus`) 자동 생성 — 회계사 확정(자기참조 채점 방지, TAX-028과 동일 원칙).
- ❌ PII·회계사 식별자 저장 — 마스킹·익명화를 수집 1단계부터.

**분할:** TAX-030-A(자동 수집 훅) → TAX-030-C(환류 스크립트) → TAX-030-B(피드백 버튼/API).
**의존성:** law-verifier(Phase 3 ✅) + 골든셋 작성 보조 도구(TAX-028 ✅). 실제 진입은 **Phase 4 이후 운영 단계**.
**연계 FR:** FR-18(골든셋 회귀 자동화)의 종착. 정식 기능 승격 시 PRD에 FR 신설·SSOT 정합 별도 티켓 필요(임의 FR 부여 금지).

---

## 3. 현재 상태 (2026-07-06 기준 — v2.7)

| 단계 | 상태 | 예상 기간 | 비고 |
|---|---|---|---|
| Phase 0 — M0 (기반 구축) | ✅ 완료 | 완료 (2026-05-12) | TAX-001~009 9건 완료, 하니스 엔지니어링 완성 |
| Phase 1 — M1 (국세 API + 테스트 인프라) | ✅ 완료 | 완료 (2026-05-15) | 국세법령 API 392건 검색 실증, Vitest + CI 그린 |
| Phase 2 — M2 (RAG 통합) | ✅ 완료 | 완료 (2026-05-17) | TAX-011-A~E 완료, GPT-4o-mini E2E 성공 (~16초) |
| **Phase 3 — M3 (law-verifier + Playwright E2E)** | ✅ **완전 완료** (2026-06-10) | 완료 | TAX-012-A~H + BUG-001~006 완료. 골든셋 40건(TAX-036). TAX-042D·050·051로 V3·V4 라벨 강화. P95 9.67s ✅ PASS (2026-06-10). Phase 4 코딩 게이트 해제 |
| **Phase 4 — M4 (벡터 DB)** | ✅ **완료** (2026-06-10) | 완료 | TAX-026-A~H 전체 완결. 코드(B~G)+Neon실연결+임베딩38건적재+스모크PASS(H). matchStage=vector 운영 경로 실동작 확정. vitest 387/387. Phase 5 착수 가능 |
| **Phase 5 — M5 (판례·예규 적재)** | ✅ **완료** (2026-06-10) | 완료 | TAX-052~055 전체 완결. 비법령 89건 적재(DB 127건) + G-2 골든셋 30건 검수·머지(golden_direct.json 66건). G-2 라벨 정확도 100%. vitest 437/437. Phase 6A 착수 가능 |
| **Phase 6A — M6A (지방세 + 시점)** | ✅ **완료** (2026-06-15) | 완료 | TAX-6A-1~11 완료. G-3 9/20 PASS 확정(11건 이연). G-4 골든셋 완료. 라벨 결정론화(TAX-6A-11). |
| **Phase 6B — M6B (부칙 + 편의 + UI)** | ✅ **완료** (2026-06-16) | 완료 | TAX-6B-1~8 전체 완결. G-5 10건 PASS. FR-17·11·12·18 완료. vitest 545/545, run_golden 105/105. PR #1 머지. |
| **Phase 7 — M7 (운영 데이터 환류 + 내용 검증기)** | 🟡 **부분 완료** (2026-07-06) | 운영 단계 (1~2주 축적 대기선) | TAX-030-A~C·TAX-6B-9 전부 완료(FR-23/24/25). 잔여=TAX-044/045(운영 데이터 1~2주 축적 후) |
| **별도 트랙 — 비법령 직접검색** | ✅ **완결** (2026-05-22) | 완료 | TAX-015~016 (판례 T4 + 해석례·국세청해석·심판례 T3). 벡터 DB 없이 OpenAPI 직접 호출. vitest 127건. 엄브렐러 TAX-016 종료 |
| **별도 트랙 — 비법령 코퍼스 완전 확장** | ✅ **완결** (2026-07-04) | 완료 | TAX-6B-13~19. 판례 10,083건·심판례 135,810건 pgvector 전량 적재(voyage-4). 해석례 목록전용 전환(P95 개선). P95·정확도 정량 회귀는 회계사 결정으로 생략 |
| **별도 트랙 — 판례·심판례 인용 연결망** | 🟡 **부분 완료** (2026-07-06) | 완료(33만 대기) | TAX-6B-23·31·32·36·37 완결(citation_edges 132,381행 + 참고목록 1-hop확장·피인용부스트, 라이브 검증 완료). TAX-6B-33(OVERRULED 검수 큐)은 회계사 승인 대기 |
| **별도 트랙 — 검색 정확도 지속 개선 시리즈** | ✅ **완결** (2026-07-02) | 완료 | TAX-6B-10~12·24~30. 재현율·정렬 결함 10건 순차 수정(법리축 분리·본문 관련도 반영·다중쿼리 병합 등). vitest 610→673 누적 GREEN |
| **별도 트랙 — 비법령 검색 정확도 후속 개선** | 🟡 부분 진행 (TAX-043 완료 2026-06-08) | 단계 2는 운영 1~2주 후 진입 | 단계 1 옵션 B+C 완결(불용어 29개·사건번호 정확매칭). 단계 2 TAX-044(도메인 사전, ⭐⭐⭐)·단계 3 TAX-045(패널티+사후검증, ⭐⭐) 운영 데이터 대기 |
| **품질 개선 트랙 — V3·V4 라벨 강화 + P95** | ✅ **완결** (2026-06-10) | 완료 | TAX-042D(V3 라벨 결정 표) → TAX-050(V4 시점 결정 트리) → TAX-051(V3 어댑터 후처리). vitest 387/387. P95 누적 9.67s ✅ PASS |

**Phase 3 결함 수정 현황 (2026-05-18 재평가 → BUG 티켓화·구현):**

| 티켓 | 결함 | 상태 |
|---|---|---|
| BUG-001 | V5 면책 고지 자동 부착 | ✅ 구현완료 |
| BUG-002 | V2 summary 환각 검사 | ✅ 구현완료 |
| BUG-003 | V4 시점 형식 + V5 면책 문구 일치 검증 | ✅ 구현완료 (회계사 결정 옵션 A) |
| BUG-005 | 노출 게이트 화이트리스트 + 빈 발췌 V2 FAIL + 주석 정리 | ✅ 구현완료 |
| BUG-006 | 골든셋 네거티브 확충 (G-N2/N3/N4) | ✅ 구현완료 |
| BUG-004 | V1 시점·content 일치 + 재검색 쿼리 | ⏸ Phase 4 보류 (회계사 결정) |

> 근거 리포트: `docs/reports/PHASE3-EVALUATION_2026-05-18_report.md`, `docs/reports/BUG-00X_report.md`

**품질 개선 트랙 완료 현황 (Phase 3 품질 후속, 2026-06-10 완결):**

| 티켓 | 내용 | 상태 |
|---|---|---|
| TAX-031·032 | 법령 검색 정확도(정확 매칭 + 조문 조립) | ✅ 완료 (2026-05-24) |
| TAX-036 | 골든셋 40건 완성 (목표 30건 초과 달성) | ✅ 완료 (2026-06-05) |
| TAX-037~039 | 비법령 V4 `[결정:YYYY.MM.DD]` 3단 체인 | ✅ 완료 (2026-06-05) |
| TAX-042D | SYSTEM_PROMPT [라벨 결정 표] + TIER_ALLOWED_LABELS 단일 진실원천 | ✅ 완료 (2026-06-08) |
| TAX-043 | 비법령 자연어 정규화 (불용어 29개 + 사건번호 정확매칭) | ✅ 완료 (2026-06-08) |
| TAX-050 | SYSTEM_PROMPT 시점 라벨 결정 트리 (V4 강화) | ✅ 완료 (2026-06-09) |
| TAX-051 | 어댑터 후처리 V3 안전망 + P95 9.67s ✅ PASS | ✅ 완료 (2026-06-10) |

**다음 즉시 작업 / 잔여 (2026-06-10 갱신):**

**✅ Phase 3 잔여 전체 완료:**
- ✅ 골든셋 40건 완성 (TAX-036, 2026-06-05)
- ✅ P95 100회 재측정 — 누적 9.67s PASS (TAX-051, 2026-06-10)
- ✅ 심판례 T3 등급표 명문화 (TAX-017)
- ✅ PRD·SSOT·CLAUDE.md 정합 (TAX-017)

**→ Phase 4(TAX-026-B~) 코딩 게이트 해제 — pgvector + OpenAI 임베딩 착수 가능**

**✅ Phase 4(TAX-026-B~H) 완료 (2026-06-10):**
- ✅ 코드 구현(B~G) + Neon 실연결 + 임베딩 38건 적재 + 스모크 PASS(H)
- ✅ `.env.example` `DATABASE_URL=` 주석 해제

**✅ Phase 5(TAX-052~055) 완료 (2026-06-10):**
- ✅ collectNonlaw.ts 수집 파이프라인 + 비법령 89건 임베딩 적재 (DB 127건)
- ✅ 벡터 fallback 비법령 통합테스트 12건
- ✅ G-2 골든셋 30건 검수·머지 — golden_direct.json 40→66건, G-2 라벨 정확도 100%
- ✅ draft 파일(`golden_direct_nonlaw.draft.json`) 삭제 — status 이중 집계 해소

**→ Phase 6A 착수 가능 (지방세법령 API 통합 + 시점 검색 + G-3·G-4 골든셋)**

**✅ Phase 6A 진입 전 잔여 처리 완료 (2026-06-11):**
- ✅ TAX-056 베타 접근 게이트 구현·Vercel 배포 완료 (proxy.ts+HMAC 세션, vitest 450/450)
- ✅ UI E2E Playwright storageState 인증 셋업 추가 — 6/6 PASS (TAX-056 베타 게이트 차단 해소)

**✅ Phase 6A 완료 (2026-06-15):**
- ✅ TAX-6A-1: 지방세법령 API 진단 — 국세API 동일 접근, LOCAL_TAX_API_KEY 불필요
- ✅ TAX-6A-2·3: 취소 (지방세 별도 어댑터 불필요 확인)
- ✅ TAX-6A-4~7: 시점 로직·UI·G-3·G-4 골든셋 구현
- ✅ TAX-6A-8: False Premise Defense (전제 검증 지침, G4-03·04·06 PASS)
- ✅ TAX-6A-9: G-3 법령 연혁 API — 방안 A 완결. G-3 9/20 PASS. vitest 95/95
- ✅ TAX-6A-10: G-3 V-FAIL 진단 + V3 안전망 양방향 확장
- ✅ TAX-6A-11: 라벨 결정론화(처방 D) + temperature 고정(처방 F) — G-3 비결정성 완결
- ⏸ G-3 나머지 11건 (구조적 V-FAIL, 별도 티켓 TAX-6A-10 이연)

**✅ Phase 6B 완료 (2026-06-16):**
- ✅ TAX-6B-1~2: 부칙·경과조치 (FR-17 백엔드·프론트엔드)
- ✅ TAX-6B-3: 최근 검색어 + PII 마스킹 (FR-11)
- ✅ TAX-6B-4: 즐겨찾기·북마크 (FR-12)
- ✅ TAX-6B-5: UI 다듬기 (접근성·키보드 단축키)
- ✅ TAX-6B-6~7: G-5 폐지 골든셋 10건 실측·검수·PASS 확정
- ✅ TAX-6B-8: FR-18 골든셋 회귀 CI (golden.yml)
- ✅ Vercel 환경변수 등록 + 베타 배포 + 로그인 테스트 완료
- ✅ PR #1 머지 (fix/tax-6a-10-g3-verify-diagnosis → master, 2026-06-16)

**✅ Phase 7(운영 환류) 부분 완료 + 비법령 코퍼스·인용 연결망·검색 정확도 트랙 완결 (2026-06-17 ~ 2026-07-06):**
- ✅ TAX-030-A~C(운영 쿼리 로그·조용한 틀림 신고·환류 스크립트) + TAX-6B-9(내용 검증기) 완료
- ✅ TAX-6B-13~19(판례·심판례 pgvector 전량 적재, voyage-4, 해석례 목록전용 전환)
- ✅ TAX-6B-10~12·24~30(검색 정확도 지속 개선 10건)
- ✅ TAX-6B-23·31·32·36·37(인용 연결망 추출·적재·참고목록 배선, 라이브 검증 완료)
- ✅ TAX-6B-34·35(심판례·판례 주간 증분 수집기)

**잔여 (선택·저우선):**
- ⏸ BUG-004 V1 시점·content 일치 — Phase 4 보류 유지 (회계사 결정)
- ⬜ 비법령 검색 정확도 단계 2·3 (TAX-044·045) — 운영 데이터 1~2주 확보 후
- ⬜ TAX-6B-33 (OVERRULED 검수 큐) — 회계사 승인 대기
- ⬜ 해석례 코퍼스 적재(TAX-6B-20~22, 설계만 존재) — 회계사 승인 대기
- ⬜ P95 answer tail latency 후속 — 벡터 무관 확정, 병목은 gpt-4o-mini 긴 content 생성 (현재 누적 P95는 합격선 내)

**Phase 3 회계사 노출 정책:**
- ✅ **V1~V6 검증 통과 답변은 회계사에게 노출 가능**
- ❌ 검증 FAIL(E-VERIFY-FAIL)·미검증(PENDING) 답변은 노출 금지 — UI에서 자동 차단 (BUG-005 화이트리스트로 강화)

---

## 4. 마일스톤 우선순위 매트릭스

SSOT §11과 PRD §5의 우선순위를 통합한 매트릭스입니다.

### 4.1 P0 — MVP 필수 (Phase 1~3 완료 시점)

| ID | 기능 | 관련 Phase |
|---|---|---|
| FR-1 | 국세법령 API 직접 매칭 검색 | Phase 1 |
| FR-3 | 자연어 쿼리 변환 (LLM 1차) | Phase 2 |
| FR-4 | 검색 결과 → 자연어 답변 생성 (LLM 2차) | Phase 2 |
| FR-5 | 답변 라벨링 (🟢/🟡/⚪/⚫) | Phase 2 |
| FR-6 | law-verifier 검증 레이어 (V1~V6) — **필수** | Phase 3 |
| FR-7 | 통합 검색 UI | Phase 2 |
| FR-8 | 원문 링크·개정일·시행일·현행 여부 표시 | Phase 1 |
| FR-14 | 인용 복사 기능 | Phase 2 |
| FR-16 | 법적 면책 고지 | Phase 2 |

### 4.2 P1 — 차기 우선 (Post-MVP, Phase 4~6B)

| ID | 기능 | 관련 Phase |
|---|---|---|
| FR-2 | 지방세법령 API 검색 | Phase 6A |
| FR-9 | 벡터 DB 의미 유사도 검색 | Phase 4 |
| FR-10 | 판례·예규 적재·임베딩 | Phase 5 |
| FR-15 | 시점 검색 (과거 시점 적용) | Phase 6A |
| FR-17 | 부칙·경과조치 자동 연결 | Phase 6B |
| FR-18 | 골든셋 회귀 테스트 자동화 | Phase 3~6B 점진 |

### 4.3 P2 — 편의 기능 (조기 진입 가능)

| ID | 기능 | 비고 |
|---|---|---|
| FR-11 | 최근 검색어 (로컬 저장) | Phase 3 끝에 조기 진입 가능 (PRD §5.1.3) |
| FR-12 | 즐겨찾기·북마크 | 클라이언트 측 구현 가능 |

### 4.4 P3 — 후순위

| ID | 기능 |
|---|---|
| FR-13 | 검색 결과 PDF·노트 내보내기 |

### 4.5 보류 (현 단계에서 배제 — SSOT §11.3)

- 사용자 계정·로그인
- 유료 결제·구독
- 다국어 지원
- 모바일 네이티브 앱
- 자동 세무 계산·신고
- 세무 자문
- 의뢰인 정보 저장

---

## 5. 의존성 그래프

```
┌──────────────────────────────────────────────────────────────┐
│  Phase 0 (M0)  ✅  완료 (2026-05-12)                          │
│  프로젝트 기반·하니스                                          │
│  TAX-001~009                                                 │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 1 (M1)  ✅ 완료 (2026-05-15, $0)                       │
│  국세법령 API + 검색 결과 스키마                               │
│  + ★ Vitest + CI 테스트 인프라 도입 ★                          │
│  · TaxLaw 도메인 엔티티                                       │
│  · nationalTaxLaw 어댑터                                      │
│  · 단위·통합 테스트 + PII 필터 테스트                          │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 2 (M2)  ✅ 완료 (2026-05-17, GPT-4o-mini)              │
│  RAG 통합: 자연어 변환 + 답변 생성 + 라벨링 + Trust Tier       │
│  + 인용 복사 + 면책 고지 + E2E ~16초 (P95 재측정 필요)          │
│  ⚠️ 회계사 노출 금지 (검증 레이어 미통합)                       │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 3 (M3)  ✅ 엔지니어링 완료 (2026-05-17)  ⭐ 회계사 노출 가능 │
│  law-verifier V1~V6 통합 · 75 tests · 5 E2E PASS             │
│  잔여: 회계사 골든셋 30건 작성 + P95 재측정                    │
│  · 검증 FAIL 답변 노출 금지 (자동 차단)                        │
└─────────┬──────────────────────────┬─────────────────────────┘
          │                          │
          ▼                          ▼
┌────────────────────────┐  ┌──────────────────────────────────┐
│  Phase 4 (M4) ✅       │  │  (병렬 가능) P2 조기 진입 후보    │
│  완료 (2026-06-10)     │  │  · 최근 검색어 (FR-11)            │
│  벡터 DB + Neon 실연결 │  │  · 즐겨찾기 (FR-12)               │
│         ║              │  └──────────────────────────────────┘
│         ║ (v1.1 신설)  │
│         ║ 부분 병렬 ──→│  ┌──────────────────────────────────┐
│         ║              │  │  Phase 5 (M5) ✅ 완료 (2026-06-10) │
│         ║ ←── 임베딩    │  │  TAX-052~055 · 비법령 89건 적재    │
│         ║   적재는      │  │  + G-2 골든셋 30건 머지(66건)      │
│         ║   순차       │  │  라벨 정확도 100% · vitest 437/437 │
└────────┬───────────────┘  └────────────┬──────────────────────┘
         │                                │
         └──────────────┬─────────────────┘
                        │ (Phase 4 + 5 모두 완료 후)
                        ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 6A (M6A) ⬜  (3~4주, $50~120/월)  ★ v1.1 분할          │
│  지방세 API(FR-2) + 시점 검색(FR-15)                          │
│  + 골든셋 G-3 시점 20건, G-4 환각 20건                         │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│  Phase 6B (M6B) ⬜  (3~4주, $50~150/월)  ★ v1.1 분할          │
│  부칙·경과조치(FR-17) + 최근검색(FR-11) + 즐겨찾기(FR-12)      │
│  + UI 다듬기 + 골든셋 G-5 폐지 10건 + 회계사 분기 설문         │
└──────────────────────────────────────────────────────────────┘
```

**핵심 의존성 규칙:**
- Phase 1 → 2 → 3은 **순차 진행**. 검증 레이어(Phase 3) 통과 전 회계사 노출 금지.
- **Phase 4와 Phase 5는 부분 병렬 가능** (v1.1 신설): Phase 4의 벡터 DB 인프라 구축과 Phase 5의 데이터 수집·정규화가 동시 진행 가능. 단 **임베딩 적재는 벡터 DB 완성 후** 순차 진행.
- **Phase 6A는 Phase 4·5 모두 완료 후 진입**, Phase 6B는 6A 완료 후 진입.
- P2 편의 기능(FR-11, FR-12)은 원래 Phase 3 끝에 조기 진입 후보였으나 **v1.1에서 Phase 6B로 정리** (위치 일원화).

---

## 6. 위험 요소 및 대응

PRD §18을 정확성·운영·보안 카테고리로 재분류했습니다.

### 6.1 정확성 위험 (최우선 — §0 제품 철학 1순위)

| 위험 | 영향 | 대응 | 관련 Phase |
|---|---|---|---|
| LLM 환각으로 잘못된 조문 인용 | **법적 책임 위험** | law-verifier V1 필수, 라벨링 강제, 면책 고지 | Phase 3 |
| LLM이 발췌 텍스트를 의역 | **인용 무결성 훼손** | V2 문자 단위 검증, 시스템 프롬프트로 의역 금지 | Phase 2~3 |
| 시점 혼동 (현행을 과거 시점에 적용) | **잘못된 세무 판단** | V4 시점 라벨 의무화, 모호 시 E-TEMPORAL-AMBIGUOUS | Phase 2~3 |
| 부칙·경과조치 누락 | **신·구법 적용 경계 오인** | FR-17 부칙 자동 연결 | Phase 6 |
| 법령 개정 미반영 | 잘못된 답변 | TTL 24시간, 시행일 강제 표시 | Phase 1~ |
| LLM이 유사 사례를 직접 적용 단정 | 잘못된 의사결정 유도 | V6 단정 표현 검출, 시스템 프롬프트로 단정 금지 | Phase 3 |
| 골든셋 부재로 회귀 검출 실패 | 정확성 저하 무자각 | M3에서 G-1 30건 작성, G-2 M5, G-3·G-4 M6A, G-5 M6B로 분산 (v1.1) | Phase 3~6B |
| 검증 통과한 오답(silent failure) 무수집 | 정확도 저하 무자각·재발 | 운영 환류 트랙(TAX-030, v1.7): 회계사 "👎 신고" + 자동 FAIL 수집 → 골든셋 환류·CI 박제 | 운영(Phase 4+) |
| 단위·통합 테스트 부재로 회귀 미검출 | 코드 품질 저하·잠재 버그 | M1에서 Vitest+CI 도입, M3에서 Playwright E2E 추가 (v1.1 신설) | Phase 1, 3 |
| LLM 비용 폭증 | 운영 비용 부담 | PRD §17.3.1 단계별 월 비용 상한 표 준수, 초과 시 인간 승인 (v1.1 신설) | Phase 2~ |
| Phase 일정 지연 누적 | 회계사 노출 지연·M3 도달 지연 | 각 Phase 예상 기간 명시(v1.1), 1주 단위 진행 점검, 지연 30% 초과 시 회계사에게 보고 | 전 Phase |

### 6.2 운영 위험

| 위험 | 영향 | 대응 | 관련 Phase |
|---|---|---|---|
| 외부 API(국세청·행안부) 응답 지연·장애 | 사용성 저하 | 캐시 24시간, 명확한 에러 메시지 (E-API-TIMEOUT 등) | Phase 1, 6 |
| LLM API 비용 폭증 | 운영 비용 부담 | 자연어 쿼리 변환은 작은 모델, 답변 생성만 큰 모델 | Phase 2~ |
| 벡터 DB 임베딩 비용 | 초기 적재 비용 | 배치 임베딩, 점진적 적재 | Phase 4~5 |
| LLM 결정론성 미흡 | 같은 질문에 다른 답변 | `temperature` 0.0~0.2, `top_p` 0.9 이하, 정렬 기준 고정 | Phase 2~ |
| 회계사 노출 시점 오판 | 미검증 답변 노출 | **Phase 3 통과 전 노출 금지 원칙 엄수** | Phase 2 → 3 |

### 6.3 보안·개인정보 위험

| 위험 | 영향 | 대응 | 관련 Phase |
|---|---|---|---|
| 개인정보 검색어 노출 | **개인정보 침해** | 주민·사업자번호 패턴 필터링 (E-PII-DETECTED), 입력 거부 | Phase 1~ |
| API 키 노출 | 인증 정보 유출 | `.env.local` git 제외, Vercel 환경변수, 시크릿 스캔 도입 검토 | Phase 0~ |
| 에러 로그에 회계사 식별자 포함 | 식별자 유출 | 로그 마스킹 (SSOT §7.8, PRD §14.3) | Phase 1~ |
| 휴대폰·이메일 비마스킹 저장 | 개인정보 노출 | 저장 전 마스킹 (`010-****-****`, `u***@d***`) | Phase 6 (최근 검색어 시) |

---

## 7. 변경 이력
| 날짜 | 버전 | 내용 | 작성자 |
|---|---|---|---|
| 2026-05-13 | 1.0 | 초안 작성 — PRD v2.0 §16 마일스톤(M0~M6)과 SSOT v2.0 §11 우선순위 매트릭스를 통합. Phase 0(M0) 완료 상태 반영(TAX-001~009). Phase 1(M1) 다음 진입 명시. Phase 3(law-verifier) 통과 전 회계사 노출 금지 원칙 명시. 정확성·운영·보안 카테고리별 위험 매트릭스 정리. ASCII 의존성 그래프 추가. | Claude + 회계사 |
| 2026-05-14 | 1.1 | **ROADMAP 평가 보고서 반영 갱신 (SSOT v2.1·PRD v2.1과 동시 동기화).** 모든 Phase에 예상 기간(주 단위)·예상 월 비용(PRD §17.3.1 상한) 컬럼 추가. **Phase 1에 Vitest+CI 테스트 인프라 도입 의무화**(SSOT §2, PRD §15.0 정합). **Phase 2에 응답시간 P95 사전 측정 단계 추가**(PRD §7.1 KPI 달성 가능성 사전 검증). **Phase 3에 Playwright UI E2E 도입 의무화** + 골든 시나리오 5건 명시. **Phase 6 → Phase 6A/6B 분할**(골든셋 50건과 4개 기능 과부하 분산). G-3·G-4는 6A, G-5는 6B로 골든셋 작성 시점 분산. **Phase 4·5 부분 병렬화 명시**(벡터 DB 인프라와 데이터 수집·정규화 동시 진행 가능, 임베딩 적재는 순차). Phase 2 검증 기준에 "PRD §4.1~4.6 시나리오 A~F" 출처 명시(평가 보고서 오해 정정). 위험 매트릭스에 테스트 부재·LLM 비용·일정 지연 항목 추가. G-1 골든셋 30건 유지(SSOT v2.1·PRD v2.1과 정합 — 50→30 하향 결정). | Claude + 회계사 |
| 2026-05-17 | 1.2 | **Phase 1·2 완료 반영.** Phase 1(M1): 국세법령 API Adapter 및 테스트 인프라(Vitest+CI) 완료, 실제 API 392건 검색 실증. Phase 2(M2): TAX-011-A~E 완료(도메인 모델·QueryRewriter·AnswerGenerator·Usecase·UI), LLM Gemini → GPT-4o-mini 전환, E2E ~16초(P95 목표 미달 재측정 필요). Phase 3(M3)를 다음 진입으로 지정. | Claude + 회계사 |
| 2026-05-17 | 1.3 | **Phase 3 엔지니어링 완료 반영.** TAX-012-A~H 8개 서브태스크 완료: lawVerifierPort(A), LawVerifierAdapter V1~V6(B), generateAnswer 5단계 통합·재시도 정책(C), UI PENDING 차단·E-VERIFY-FAIL(D), law-verifier.md 에이전트 정의(E), golden_direct.json·eval/README.md·골든셋 테스트 러너(F), Playwright E2E 5건(G), CI e2e job(H). Vitest 75 tests + E2E 5 tests 전체 그린. 잔여: 회계사 골든셋 30건 작성·P95 재측정. Phase 3 회계사 노출 정책 갱신(V1~V6 PASS 답변 노출 가능). | Claude + 회계사 |
| 2026-05-20 | 1.4 | **Phase 3 평가 및 결함 수정 완료 반영.** 2026-05-18 Phase 3(M3/TAX-012) 독립 재평가(`docs/reports/PHASE3-EVALUATION_2026-05-18_report.md`) 후 발견된 결함을 BUG-001~006으로 티켓화·구현: BUG-001(V5 면책 자동부착)·BUG-002(V2 summary 환각검사)·BUG-003(V4 시점형식·V5 면책문구 검증, 회계사 결정 옵션 A)·BUG-005(노출 게이트 화이트리스트·빈 발췌 V2 FAIL·주석 정리)·BUG-006(골든셋 네거티브 G-N2/N3/N4 확충). BUG-004(V1 시점·content 일치)는 Phase 4 보류. 골든셋 "6건"→"10건(PASS 6 + 네거티브 4)" 정합. §3 잔여 작업을 A.회계사·운영 / B.방향 결정 후 AI / C.AI 바로 착수로 재분류. 신규 잔여: 시점라벨 사양↔코드 표기 정합 티켓(BUG-003 §7.1). | Claude + 회계사 |
| 2026-05-22 | 1.5 | **비법령 직접검색 트랙(TAX-015~016) 완결 반영.** §2에 "별도 트랙 — 비법령 직접검색(Off-roadmap)" 섹션 신설: 벡터 DB 없이 국세법령정보 OpenAPI 직접 호출로 판례(`prec`·T4)·법령해석례(`expc`·T3)·국세청 해석(`ntsCgmExpc`·T3)·조세심판원 결정례(`ttSpecialDecc`·T3)를 검색 결과에 편입. 본문 있는 자료는 발췌 인용(V1~V6), 본문 없는 자료는 참고 목록(references·V검증 비대상). `TaxLaw.sourceType`+비법령 필드·`LabeledAnswer.references` 추가, 새 환경변수 없음(기존 OC 키 재사용·A′ 경로). 테스트 127개. Phase 4/5(벡터 DB)와는 대체 아닌 보완·선행 관계 명시. §3 현재 상태 표·잔여 작업 갱신(B-5 심판례 T3 등급표 명문화 / C-8 PRD 정합 반영 추가). 엄브렐러 TAX-016 종료. | Claude + 회계사 |
| 2026-05-22 | 1.6 | **TAX-017 상위 문서 정합 완료 반영.** prd-writer 검토(2026-05-22)에 따라 비법령 트랙을 SSOT v2.2 → PRD v2.2 → CLAUDE.md 순으로 정합(문서 위계 역전 해소). SSOT §7.6/PRD §6.1/CLAUDE.md §6.2 등급표에 **심판례 T3 명문화**, PRD §5.2 **FR-19/20 신설**(MAX_REFERENCES "조정 가능" 표기), V1 식별자 분기, references V검증 비대상 규칙, §9.3 "직접 매칭 라벨은 Trust Tier를 따른다" 보정, §16 별도 트랙 행. 3문서 충돌 0·코드 변경 없음. 별도 트랙 §잔여·후속 및 §3 잔여 작업(B-5·C-8) 완료 표시. 리포트 `docs/reports/TAX-017_report.md`. | Claude + 회계사 |
| 2026-06-08 | 1.8 | **비법령 검색 정확도 후속 개선 트랙 신설.** §2에 "별도 트랙 — 비법령 검색 정확도 후속 개선(운영 데이터 환류)" 섹션 추가 — 상위 별도 트랙(비법령 직접검색)의 연속선으로 위치(A-2 형식). 단계 1 TAX-043(옵션 B 불용어 29개 + 옵션 C 사건번호 정확매칭) 완료(2026-06-08), 단계 2 TAX-044(옵션 A 세무 도메인 사전 + 결합 검색, ⭐⭐⭐) 운영 1~2주 후 진입, 단계 3 TAX-045(옵션 D 패널티 + E 사후 검증, ⭐⭐) TAX-044 완료 후 진입. 변수 분리 원칙 명시(추측 기반 사전 방지). §3 현재 상태 표에 행 추가. 근거: `docs/reports/TAX-042E_nonlaw-search-insights.md`·`TAX-043_report.md`. | Claude + 회계사 |
| 2026-05-23 | 1.7 | **Phase 4 설계·골든셋 보조 완료 + 운영 환류 트랙 계획 반영.** Phase 4(벡터 DB) 설계 게이트 통과(TAX-026-A, 회계사 결정 4개 확정: THRESHOLD=3·신규 usecase·OpenAI 임베딩·pgvector) 및 B~H 서브태스크 등록. **골든셋 작성 보조 인프라(TAX-028) 구현 완료** — 시드→원문 자동 fetch→V1·V2 골격 생성(`scripts/golden/buildCases.ts`)·현황 리포트(`status.ts`)·시드 템플릿(`golden_seeds.json`), 정답(summary)은 회계사 작성 강제(자기참조 방지). 후속 P95 재측정(TAX-029)·운영 환류(TAX-030) 티켓 초안화. §2에 **"향후 트랙 — 틀린 케이스 → 골든셋 환류"** 섹션 신설(자동 FAIL 수집 + 회계사 "👎 신고"로 silent failure 수집 → buildCases 골격화 → 검수 → 머지 → CI 박제, 정답 자동생성·PII 저장 금지). §6.1 위험에 "검증 통과한 오답 무수집" 행 추가. 리포트 `docs/reports/TAX-028_report.md`. | Claude + 회계사 |
| 2026-06-10 | 1.9 | **품질 개선 트랙 완결 + Phase 4 코딩 게이트 해제.** TAX-036(골든셋 40건 완성, 2026-06-05), TAX-037~039(비법령 V4 결정 라벨 3단 체인), TAX-042D(V3 라벨 결정 표 + TIER_ALLOWED_LABELS 단일 진실원천), TAX-043(비법령 자연어 정규화), TAX-050(SYSTEM_PROMPT 시점 라벨 결정 트리 — V4 0건 달성), TAX-051(어댑터 후처리 V3 안전망 — T3·T4→🟢 강제 다운그레이드) 완료. vitest 387/387 PASS. **TAX-029 P95 100회 재측정 누적 9.67s ✅ PASS(합격선 15s)** — Phase 3 잔여(골든셋 30건+P95 재측정) 전체 해소. Phase 4(M4, TAX-026-B~) pgvector + OpenAI 임베딩 **코딩 게이트 해제**. §3 현재 상태 표·잔여 작업 갱신. §2 Phase 4 상태 🟡 게이트 해제. §2에 "품질 개선 트랙" 행 신설. 리포트 `docs/reports/TAX-051_report.md`. | Claude + 회계사 |
| 2026-06-10 | 2.1 | **Phase 5(판례·예규 적재) 완결 반영.** TAX-052~055 전체 완료(2026-06-10): collectNonlaw.ts 수집 파이프라인(5세목 18키워드) → 비법령 89건 임베딩 적재(DB 합계 127건: 법령34+심판례86+해석례4+판례3, embed.ts BATCH_SIZE 버그픽) → 벡터 fallback 비법령 통합테스트 12건 → G-2 골든셋 30건(시드→AI summary 초안→회계사 검수→`golden_direct.json` 머지 40→66건). **G-2 라벨 정확도 30/30=100% (합격선 ≥95% 충족)**, golden:status 66건 V1~V6 전체 PASS, vitest 437/437. 계획상 `golden_similar.json` 별도 파일 대신 `golden_direct.json` 통합(동일 러너·status 재사용). draft 파일 삭제로 status 이중 집계 해소. §2 Phase 5 섹션 ⬜→✅, §3 현재 상태 표·잔여 작업, §5 의존성 그래프 갱신. Phase 6A 착수 가능. 리포트 `docs/reports/TAX-052_report.md`~`TAX-055_report.md`. | Claude + 회계사 |
| 2026-06-16 | 2.5 | **Phase 7(운영 데이터 환류) 정식 단계 신설 — 회계사 결정 3건 반영.** §2에 **Phase 7 — M7** 섹션 신설(운영 쿼리 로그 자동 수집 FR-23 + 조용한 틀림 신고 FR-24 + 내용 검증기 FR-25 + 골든셋 환류 TAX-030-C + 비법령 정확도 TAX-044/045). 그동안 "향후 트랙(Off-roadmap)"으로만 있던 운영 환류(TAX-030)를 정식 Phase로 승격(하위 호환 메모로 기존 두 별도 트랙 통합 명시). 회계사 결정 3건: ①저장소=기존 Neon Postgres 재사용(파일 저장은 Vercel 서버리스 휘발로 폐기, 신규 환경변수 없음) ②수집 범위=성공 쿼리 포함 전부(TAX-044 도메인 사전 전수 작성용) ③내용 검증기=방안 A(규칙 기반 `expectedContent` 대조). 구현 순서 TAX-030-A(임계 경로)→030-B→6B-9→030-C→(데이터 1~2주 대기선)→044→045. §3 현재 상태 표에 Phase 7 행 추가. 불변 제약: 정답 자동생성 금지·PII/식별자 미저장·V1~V6 무변경·fail-soft. PRD v2.5·SSOT v2.6과 동기. 코드 변경 없음(문서 정합 전용). | Claude + 회계사 |
| 2026-06-21 | 2.6 | **voyage-4 운영 기준 및 심판례 코퍼스 확장 추적 반영.** §0 임베딩 기준을 voyage-4(1024차원)로 갱신. §3 현재 상태 표에 "별도 트랙 — 비법령 코퍼스 확장" 행 추가(TAX-6B-14~16 판례 DB 참고 목록·전량 적재 완료, TAX-6B-18A 심판례 수집기 선행 구현). 실제 심판례 전수 수집·임베딩·검색 전환은 저장소/비용 승인 전 보류로 명시. SSOT v2.7·PRD v2.6과 동기. | Codex (미승인, 검토 후 유지) |
| 2026-07-06 | 2.7 | **Phase 7 완료 반영 + 비법령 코퍼스 완전 확장·인용 연결망·검색 정확도 시리즈 대량 반영.** 2026-06-21 이후 완료된 TAX-6B-9~37 시리즈(28개 티켓)를 반영. ①**Phase 7** 상태 ⬜→🟡 부분 완료: TAX-030-A~C(운영 쿼리 로그·조용한 틀림 신고·환류 스크립트)·TAX-6B-9(내용 검증기) 완료 확인, 잔여는 TAX-044/045(운영 데이터 대기)뿐. ②**"별도 트랙 — 비법령 코퍼스 완전 확장"** 신설(✅ 완결): TAX-6B-13~19 — 판례 10,083건·심판례 135,810건 pgvector 전량 적재(voyage-4), 해석례 목록 전용 전환. P95·정확도 정량 회귀는 회계사 결정으로 생략(LLM 과금). ③**"별도 트랙 — 판례·심판례 인용 연결망"** 신설(🟡 부분 완료): TAX-6B-23(PoC)·31(엣지 추출·적재, citation_edges 132,381행)·32(참고목록 1-hop 확장+피인용 부스트, 라이브 검증 완료)·36·37(인용 원천 필드 보존) 완결, TAX-6B-33(OVERRULED 검수 큐)은 회계사 승인 대기로 명시. ④**"별도 트랙 — 검색 정확도 지속 개선 시리즈"** 신설(✅ 완결): TAX-6B-10~12·24~30 재현율·정렬 결함 10건 순차 수정. ⑤TAX-6B-34/35(심판례·판례 주간 증분 수집기) 반영. ⑥TAX-6B-20~22(해석례 코퍼스 적재)·TAX-6B-33은 설계만 존재·미착수로 명시(회계사 승인 대기). §3 현재 상태 표·§4 다음 즉시 작업 갱신. 코드 변경 없음(문서 정합 전용). PRD v2.8과 동기(예정). | Claude + 회계사 |
| 2026-06-16 | 2.4 | **Phase 6A·6B 전체 완결 + 베타 배포 완료.** TAX-6A-10(G-3 V-FAIL 진단·V3 양방향 안전망)·TAX-6A-11(라벨 결정론화·temperature 고정, G-3 비결정성 완결) 완료. Phase 6B: TAX-6B-1~8 전체 완결(부칙 FR-17·최근검색 FR-11·즐겨찾기 FR-12·UI 다듬기·G-5 폐지 골든셋 10건·FR-18 골든셋 회귀 CI). vitest 545/545·run_golden 105/105. Vercel 환경변수 4개 등록 + 베타 배포 + 로그인 실동작 확인. PR #1 머지(fix/tax-6a-10-g3-verify-diagnosis→master). §2 Phase 6A·6B 상태 ⬜→✅. §3 현재 상태 표·잔여 갱신. 잔여: TAX-6B-9 내용검증기(Phase 7 후 결정)·비법령 정확도 TAX-044(운영 1~2주 후). | Claude + 회계사 |
| 2026-06-14 | 2.3 | **TAX-6A-9 G-3 법령 연혁 API 완결.** 국세법령정보시스템 API 연혁 조회 구조적 불가 확인 → 회계사 방안 A 승인(케이스 재설계). G-3 20건 현행 API 처리 가능 케이스로 전면 재구성(rebuild_g3.mjs). lawVerifier.ts: TEMPORAL_LABEL_PATTERNS에 단일날짜·종료일없는범위 패턴 추가(방안 X 승인), V2 content=0 비법령 면제 추가. 재실측 9/20 PASS answer 병합(merge_g3_answers.mjs) + disclaimer V5 수정(하드코딩→DISCLAIMER 상수 일치). vitest 95/95 GREEN. §3 현재 상태 표·잔여 작업 갱신. 리포트 `docs/reports/TAX-6A-9_report.md`. | Claude + 회계사 |
| 2026-06-11 | 2.2 | **TAX-056 베타 접근 게이트 완결 + E2E 인증 셋업 추가.** TAX-056: proxy.ts(Edge 경비원, HMAC-SHA256 세션 서명·검증, 30일 쿠키) + login/logout API + SSOT §1.3 베타 접근 제어 명시. Vercel Production 배포 완료(`https://tax-search-system.vercel.app`), 로그인 실동작 확인. E2E 인증 셋업: TAX-056 베타 게이트로 Playwright G-1~G-5가 `/login` 리다이렉트되던 문제 해결 — `auth.setup.ts`(page.evaluate fetch+credentials:include → storageState) + `playwright.config.ts`(.env.local 파싱 + setup→chromium 의존성) + `.gitignore`(`.auth/`). vitest 450/450 · Playwright 6/6 PASS. §3 현재 상태·잔여 갱신. | Claude + 회계사 |
| 2026-06-10 | 2.0 | **Phase 4(벡터 DB) 완결 반영.** TAX-026-B~H 전체 완료(2026-06-10): 코드 구현(B~G) + Neon pgvector 실연결 + `taxlaw_embeddings` 마이그레이션 + 임베딩 38건 전량 적재 + 운영 벡터 스모크 테스트 ✅ PASS(matchStage=vector 운영 경로 실동작). `.env.example` `DATABASE_URL=` 주석 해제 완료. vitest 387/387. §2 Phase 4 섹션 상태 🟡→✅ + 산출물·검증 체크박스 갱신. §5 의존성 그래프 Phase 4 ⬜→✅. §3 현재 상태 표·잔여 작업 갱신. Phase 5 착수 가능. 리포트 `docs/reports/TAX-026-H_report.md`. | Claude + 회계사 |

---

## 8. 관련 문서

| 문서 | 역할 | 위치 |
|---|---|---|
| **SSOT** | 헌법 — 코드·아키텍처 강제 규칙 | `docs/SSOT.md` |
| **PRD** | 사양서 — 제품 사양·기능·KPI | `docs/PRD.md` |
| **CLAUDE.md** | AI 행동 1페이지 요약 | `CLAUDE.md` |
| **티켓 양식** | 작업 단위 템플릿 | `docs/tickets/_TEMPLATE.md` |
| **티켓 디렉토리** | 발급된 티켓 목록 | `docs/tickets/` |
| **리포트 디렉토리** | 구현 완료 리포트 | `docs/reports/` |
| **law-verifier** | 검증 서브에이전트 정의 (Phase 3에서 작성) | `.claude/agents/law-verifier.md` |
| **골든셋** | 정확성 평가셋 (Phase 3~6에서 작성) | `eval/` (G-1~G-5) |
| **연구 리포트** | PRD·SSOT 통합 분석 | `docs/reports/RESEARCH_2026-05-08_PRD-SSOT-analysis.md` |
| **메모리** | AI 협업 기억 체계 | `~/.claude/projects/.../memory/MEMORY.md` |

---

**문서 끝.**

> 본 로드맵은 PRD/SSOT 변경 시 함께 갱신합니다. 우선순위 매트릭스(§4) 또는 마일스톤 구조 변경 시에는 별도 티켓으로 처리하고 변경 이력(§7)에 기록합니다.
